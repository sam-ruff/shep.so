# Shep TODO

Active requests from this chat. Add new requests here immediately, including corrections. Keep an item until implementation and relevant automated/native verification are complete; remove it only while recording its evidence in [the completion log](docs/COMPLETION.md). [The request audit](docs/REQUEST_AUDIT.md) maps the complete conversation, including work already delivered. The full product goal is still active.

## Current UI and interaction work

- [ ] **R82 — Native new-mail notifications:** popups, sound and sender/subject details default on, with independent searchable Preferences controls and Test notification. Persistent initial-import/epoch/identity tracking, a coalescing background delivery worker, Linux native protocol and Windows/macOS adapters are implemented. Seven SQLite and ten worker/UI/protocol tests pass; native tests cover defaults/privacy/independent outputs, restart/arrival deduplication, compact dark layout and slow failure/retry with navigation. Windows GNU cross-target compilation passes. Notification failure tests also exposed and fixed rejected IMAP SEARCH/FETCH responses being accepted as empty success. Installed and pushed in ded5aca: 444 Rust plus two drawing-adapter tests, 44 Python tests, hooks and release/installer checks pass. The full native run passed 153/154 scenarios; its stale-modal shortcut test has a passing corrected rerun, with the rapid-key ordering edge tracked under R63. All four notification flows passed. Keep open for actual Windows/macOS delivery, Mac app-bundle integration and desktop sound/popup review; fixture tests never contact the user's notification or audio service. Default arrival scope is new unread Inbox mail; initial imports remain quiet.

- [ ] **R73 — Moved mail missing from destination:** Installed/pushed in a81d767 (building on acb33c0): immediate destination projection/cached reading, durable original-MIME and acknowledged-UID recovery, bounded background lookup, explicit Retry/review and confirmed Keep local copy controls. Retained copies use local identities and never reuse obsolete provider UIDs. Tests cover failure/retry, restart, navigation, account removal and close; all 167 native functional scenarios, 484 Rust tests plus two drawing-adapter tests, 45 Python tests, Clippy, hooks and release/installer checks pass. Remaining: actual adapter wire/journal integration, broader Undo/bulk/folder-history lifecycle checks (including repeated-move aliases), independent-process coordination alongside R01, and the personal A. Keep → Inbox report alongside R47. Preserve originals on failed or unconfirmed moves; do not equate tagged MOVE NO with proof of no effects.

- [ ] **R70 — Dock/taskbar new-mail badges:** show an unread Inbox count on the Shep launcher/dock/taskbar, including the GNOME-style badge in the user's screenshot where the desktop supports it. Reconcile new arrivals, read/unread, optimistic moves/deletes/Undo, account scope and restart without stale counts; allow disabling badges in Preferences. Use native platform integration with isolated automated tests and document actual desktop/platform support.
  Linux Unity/Dash-to-Dock protocol adapter and searchable preference are implemented with a coalescing background worker and real private-bus tests. Global count projection observes pending messages outside the current page. All 108 native scenarios, 313 Rust tests and 29 Python tests pass; release packaging is verified. Installed and shipped in 90776fa; evidence is recorded in the completion log. Keep R70 open for Windows/macOS adapters and execution, actual desktop rendering review, and remaining ambiguous-provider/restart reconciliation alongside R50/R60.

- [ ] **R63 — E2E coverage audit:** primary/secondary clear controls, remapping, capture cancellation and persistence are shipped in 742b21e. Continue mapping every remaining visible feature to realistic native happy/error/recovery scenarios as the TODO features land; complete a final functionality-path coverage audit. Installed keyboard-ordering fix b451777 removes delayed Escape/click ordering and stale asynchronous focus replies: rapid Move/Escape fails on the preceding executable and passes with the fix. Three native-widget tests and 12 targeted native flows cover ordering, text-field isolation, Find, remapping, selection and context menus. 495 Rust plus two drawing-adapter tests, 47 Python tests, Clippy/hooks, Windows compilation, release/installer checks and strict docs pass. All 174 native scenarios have passing coverage across the full run and targeted reruns, including the strengthened list/full-reader isolation test; the full run alone passed 172/174. See the completion log for the two failures and rerun evidence. Preserve the rapid-input scenarios without inserting waits between those deliberately adjacent inputs. The final functionality-path audit remains open.

- [ ] **R50/R60 — Immediate feedback:** archive/move, flags, read/unread and other reversible actions show their expected result immediately, before network/SQLite completion. Archiving removes the mail from the current list immediately; restore it with an actionable error on failure. This is the app-wide optimistic interaction principle, recorded in AGENTS.md. Commit 742b21e ships immediate flags/read and same-account moves, coalescing, stale results and rollback with unit/protocol/native verification. Commit d3a530a additionally keeps metadata actions available while bodies load and rejects stale-body targets. Commit 9c907d2 adds immediate cross-account source feedback, typed completion/rollback and ordering behind source read/flag changes. Remaining: reconcile optimistic membership/counts when switching filtered folders, ambiguous cross-account outcomes, other reversible controls, and durable restart recovery for pending actions.
- [ ] **R47 — A. Keep → Inbox:** investigate the reported failed move, verify the actual provider path and destination refresh. Commit 590ab10 ships Inbox labeling, send-queue feedback and a spaced-folder protocol test preserving acknowledgment after logout failure; the personal-account root cause is not yet confirmed.

## Folders and composition

- [ ] **R30 — Folder context menus:** delete folders and move them inside another folder; review destructive scope, keep account/server/cache state consistent, and test failure/retry behavior.
  Backend checkpoint 5eabb52 is installed and pushed: reviewed subtree plans, checked IMAP LIST/RENAME/DELETE, durable per-step acknowledgment/recovery, atomic cache/history migration and account-removal integration have protocol/store tests. Still connect the native menu, destination/deletion review, optimistic presentation, visible recovery and close controls; complete POP3 local hierarchy setup, native failure/retry/restart scenarios and shipping before removing this item.
- [ ] **R77 — Composer typing artifacts:** bottom-edge glyph clipping is corrected and installed/pushed in ebddf54, with a renderer regression that failed before the fix and clean light/dark compact native typing captures. Preserve these checks and finish the visual review after R35 inline composer integration.
- [ ] **R35 — Inline composer:** new messages/replies open in the preview pane; autosave while typing and support switching among multiple drafts and received mail without losing content, recipients or attachments. Latest clarification: replies start inline in the existing reader, with the conversation visible below (Outlook-style), and retain their open drafts per conversation when browsing elsewhere. No new reply popup. Sending must acknowledge immediately and release navigation while the durable outbox works; keep recoverable failures and unsent content visible.

## Account/settings sync and backups

- [ ] **R83 — Portable full-database export in Preferences:** export the whole SQLite database, including all cached emails/original MIME/attachments, configuration and saved account definitions, so it can be moved to another PC. Provide a clear file picker and completion/error feedback, a consistent online snapshot while the app stays responsive, and an import/restore path with schema/integrity checks and safe handling of existing data and pending operations. Preserve all database-backed state, rather than exporting only selected messages or the existing limited snapshot. Make credential portability explicit: OS-keychain passwords/tokens are outside SQLite and need a separately protected transfer mechanism or reconnection; never claim a database copy includes them. Coordinate with encrypted-cache/large-mail work (R22/R23) and credential portability (R49). Cover complete content/settings/account round trips, open-database consistency, cancellation/failure and native Preferences controls.

- [ ] **R02/R49 — Continuous Drive sync:** use OAuth and private appDataFolder for accounts and portable preferences. Offer opt-in on the first Google setup; discover an existing setup automatically on another PC; provide an ongoing toggle. Sync appearance, shortcuts, contacts and other portable settings with conflict-safe updates and offline recovery. Keep machine-specific paths/window placement local.
- [ ] **R49 — Account credentials on new PCs:** complete the password-protection decision and secure import into the OS keychain. An asynchronous question asks whether to use an additional sync passphrase (once per new PC) or Google-only protection; no answer has arrived yet. Do not claim password sync is implemented.
- [ ] **R32 — Multiple backup destinations:** polished destination list/setup for Local, Google Drive, S3, FTP/FTPS and SFTP, with independently configurable connections and easy extension points.
- [ ] **R32 — Backup options:** compression and chosen passcode encryption at setup, password prompt on restore, multiple destinations enabled together, independent upload results and configurable per-destination rolling copies (e.g. ten unreadable encrypted archives).

## Storage, appearance and final quality

- [ ] **R64 — GNOME launcher icon:** install the approved dog mark with a transparent background, matching the system theme; verify desktop/dash integration and theme changes without replacing the approved design. There are copies of the transparent icon in the shep.website folder one level up from the root
  Current sibling checkout is `../shep-website`; the inspected `assets/`, `docs/images/` and `website/dist/assets/` logo copies are opaque, as are `../shep-clients/web/public/` copies. Locate the intended alpha variants or remove only the background from the approved design.
  Built-in imagegen cutouts and their prompts are saved under ignored `artifacts/imagegen/launcher-alpha/`. The dark results have ragged edges and were rejected; the light candidate needs exact-silhouette review. No candidate has replaced the approved installed assets. Finish clean extraction before theme integration.

- [ ] **R61 — Download-and-install scripts:** provide directly runnable GitHub raw installer scripts for Linux, macOS and Windows. Default to the user home/equivalent, optionally prompt for all-user/system installation with appropriate elevation, and integrate with GNOME/KDE applications, macOS Applications and Windows Start menu. Put the commands near the top of README as the first install option and in the install docs; test download/extraction/install/update and cancellation using isolated fixtures.

- [ ] **R22 — Encrypted local cache:** encrypt SQLite/mail/attachment data at rest and safely migrate existing personal data; keychain credentials and encrypted backups alone do not fulfill this request.
- [ ] **R23 — Large mail:** remove the incoming 25 MiB, snapshot 256 MiB and preview 32,000-character ceilings using bounded streaming/paging and background large downloads. Large mail must not hold up smaller messages. Avoid replacement arbitrary caps/unbounded RAM allocations.
- [ ] **R25 — Palette editor:** configure primary, secondary, background, surface, text, accent and related theme colors in Preferences, with persistent light/dark schemes and readable states.
- [ ] **R15/R17/R21 — Final usability review:** finish remaining redundant-copy, spacing, icon sizing, alignment and compact/large-font checks, Singular message-count labels are corrected in the HTML-reader work; finish the remaining visual review. Preserve the approved dog logo, WebP assets, mouse usability, draggable panes and clean shadcn-style controls.
  The residual scale-control fragment below filtered settings is fixed in 0d2feb1, with direct renderer and saved native pixel regressions; all 109 native functional scenarios pass and the release is installed. Continue checking the previously reported clipped Shortcuts label and other compact/large-font states in the remaining visual audit.
- [ ] **R01/R06 — Provider/platform completeness:** close remaining POP3 protocol/lifecycle gaps (including malformed/duplicate UIDL), audit remaining async-imap collection helpers such as LIST for rejected-command handling, Google/CalDAV/SMTP integration evidence, independent-process coordination and Windows/macOS execution/distribution checks. Do not present fixture evidence as live-provider verification.
- [ ] **R03/R09 — Final performance gates:** keep measurements deferred while the host is busy; run backend/native gates at the end on an idle host. Do not weaken budgets. Keep quality/release CI disabled until requested; documentation publishing has a separately recorded exception in AGENTS.md.
- [ ] **R08/R10 — Ship verified work:** push at the next opportunity with a working build, as requested; do not wait for the full product backlog to be finished. Run fmt, Clippy, Rust/Python and relevant native tests; optimized release/installer verification; install for the Linux user and push main at sam-ruff/shep.so. Keep logs/artifacts out of the root and preserve personal data.
