# Shep TODO

Active unfinished requests from the desktop, mobile/browser and website sessions. Add new requests here immediately, including corrections. Keep an item until implementation and relevant automated/native verification are complete; remove it only while recording its evidence in [the completion log](docs/COMPLETION.md). [The request audit](docs/REQUEST_AUDIT.md) maps the complete conversation, including work already delivered. Read [handover.md](handover.md) before resuming. The full product goal remains unfinished.

Since the 2026-09-09 merge of `main` into `feat/mobile-web-clients`, `main` is the single integration branch for the desktop, mobile and website sessions; desktop changes arrive through `main` rather than through ports. Request numbers R67 to R80 exist twice: the desktop session and the client session numbered independently after R66. Entries below say which side a colliding number belongs to; the audit states the collision once and does not renumber history.

## Highest priority - mail sync speed and reliable moves

- [ ] **20 September app-wide immediate actions architecture:** **Approved for implementation.** Apply the [action lifecycle](docs/agents/IMMEDIATE_ACTIONS.md) consistently across individual/bulk mail, folders, calendar, drafts/sending, accounts and preferences on desktop, Flutter and browser. Update AGENTS.md and deliver production changes with regression/native evidence, not tracking alone. Retain newer intent, durable restart/Undo and conservative uncertain-result recovery. Start with the small-selection delete foreground delay; record each verified migration and keep the remaining domains explicit.

  **Full implementation goal:** Sam now explicitly requests completing and fully
  testing the architecture. The first migration is progress, not the stopping
  point. Audit every action domain/client against the approved contract and keep
  working through durable admission, execution, recovery, activity and lifecycle
  tests until the entire contract is evidenced.

  Current integration review: shared CalDAV/schema 22 prerequisites pass 19
  parser/HTTP tests, 159 native tests, default-feature checking and Clippy.
  Collection-scoped discovery, recurring-event write refusal, exact versions and
  duration/UID preservation are integrated. Native action v3 now passes 163
  tests plus the root nonmatching-inspection regression, with connection
  replacement/cleanup fences and provider routing. Finish Flutter controls and
  verify the complete client path before claiming parity. Browser calendar18
  is integrated with 250 units, 20 combined controls and 13 final Calendar/storage
  scenarios passing. Lost save/delete replies preserve current terminal status
  and newer edits. Native Activity is now in its own schema11 lane; Flutter
  folders23 and CalDAV controls continue in parallel.

  Native calendar active-action observations and predecessor lookup now use
  indexed seeks. The 100,000-history VM-step regression and seven calendar
  lifecycle integration tests pass. Keep the final combined shipping gate open.
  Browser Calendar's eight final control scenarios and production build pass,
  including all-day dates west of UTC in the grid, editor and saved request.
  Flutter checked-state adoption must retain a structured server observation,
  reject stale reviews and atomically apply that exact state; an error message
  alone cannot authorise dropping an uncertain intent.
  Fence browser read-only Calendar inspection from its starting cache revision:
  a sync or newer intent during the server read must invalidate the observation
  before it can enable adoption.
  Native calendar scheduler now seeks indexed runnable statuses; both
  100,000-history query regressions pass, including blocked children, due retry,
  independent cache repair and no runnable work.
  Flutter folder23 v2 is integrated with 178 combined Rust and seven root control
  tests passing; compact goldens match the reviewed handoff. Final combined
  shipping remains open; checked mobile rename/move/delete continues in schema24.
  The complete root Flutter suite passes 243 tests, including actual bridge and
  bulk/reader controls. Backend all-target Clippy also passes with warnings denied.
  Native/browser folder planning now uses the shared typed rejection: proven
  invalid targets need review, while offline discovery stays Waiting without
  CREATE. Root passes 13 native lifecycle tests, 13 browser units, six backend
  folder tests and two actual browser planning flows; production build passes.
  Checkpoint `8044f22` is pushed to main. Normal hooks pass 1,597 Rust executions,
  15 ignored; separately run owned calendar HTTP tests retain their evidence.
  The remaining architecture migrations and full completion audit stay active.
  Activity recovery links must open the exact retained folder/backup target,
  independently of the current recent-items page or Preferences selection.
  CalDAV credential failures must not copy raw secure-store exceptions into
  saved journal errors or visible feedback. Both corrections are in active lanes.
  Root continues browser Calendar Undo using acknowledged physical receipts,
  atomic inverse admission and newer-event ownership checks. Unknown outcomes
  still require checked recovery; keep this separate from a successful provider
  receipt and preserve any requested inverse decision across restart.
  Acknowledged browser Undo now passes nine units and three held-provider
  controls for create/edit/delete, plus build. Before shipping, verify deleted
  event restoration preserves provider fields omitted by the current portable
  event model (including recurring/attendee metadata); a partial new event must
  not be presented as a complete Undo. Unknown-result Undo decisions remain open.
  Flutter CalDAV controls are now integrated from the folder23-consolidated
  handoff. Its isolated gates pass 256 Flutter and 184 Rust tests; root combined
  tests and golden regeneration are running. Review also requires sanitising
  credential read and cleanup failures, alongside the already covered save
  failure, before shipping this continuation.
  Integrated CalDAV verification now passes 184 Rust and 256 Flutter tests;
  analysis is clean and all three regenerated compact captures match the handoff.
  Browser Undo refuses deleted-event recreation without a provider restoration
  receipt. Its nine units pass; the shared exact restoration contract remains
  active work, alongside deferred uncertain-result decisions.
  Root integrated credential recovery v3 and corrected cleanup Retry ownership
  and restart resumption. Matching Calendar inspection now retains uncertainty
  and requires explicit checked-state adoption, with no fabricated receipt.
  The same revision fence covers matching and nonmatching reads; exact identity
  and current version are mandatory. Final affected gates are running.
  Final root gates pass 185 Rust and all 260 Flutter tests; analysis, Clippy and
  strict docs pass. Browser passes all 266 units and three Undo control scenarios.
  Normal shipping hooks remain to run for this local continuation.
  Continuation `f6d9462` is pushed to main; normal hooks pass 1,597 Rust executions,
  15 ignored. Folder24 is frozen for integration. The next mobile lane addresses
  foreground/disposal fences and removes read-only connection probes from the
  global credential lifecycle FIFO. Send preparation and Calendar Undo remain
  explicit later migrations; these checkpoints do not complete the full goal.
  Root now integrates frozen mobile folder24 and desktop Activity11 together.
  Folder confirmation freezes scalar review data before 50-row background
  preparation; Activity opens exact retained recovery targets. Combined Rust,
  real controls and schema migration gates are running before shipping.
  Integrated folder24 passes all 198 mobile Rust and 265 Flutter tests, with
  clean analysis/Clippy and reviewed compact goldens. Desktop Activity's combined
  library, native build and recovery controls remain the shipping gate.
  Desktop integration passes 1,076 application library tests plus 157 shared
  tests, four actual Activity flows and six shortcut regressions. Light/compact
  dark/exact-backup captures are reviewed and Clippy passes. Shipping hooks are
  next. Mobile lifecycle v1 remains unintegrated: review found a validation-to-
  probe race after releasing the credential FIFO; the lane is adding native
  attempt-bound dispatch before handoff.

  First migration implemented: common projection outcomes, native bulk review and
  confirmation during pending individual writes, pre-admission Undo, and calendar
  save/delete with exact read-only recovery. Rust and five new native scenarios
  pass, alongside 38 native regression scenarios. Implementation is `a1049ab`;
  [completion](docs/COMPLETION.md) records the evidence. Remaining:
  durable deferred admission/calendar recovery, consistent activity and adoption
  across folders, accounts, preferences, drafts/sending, Flutter and browser.

  Checkpoint `7689ef3` is pushed and adds durable calendar admission/Waiting, native
  flag receipt repair, browser individual Activity/offline cancellation/Undo and
  Flutter durable individual admission. Browser's 90 focused unit and 18 real
  control scenarios pass; native's 40 bulk/calendar/rapid scenarios plus the new
  authentication-wait flow pass. Desktop Send now uses local queued admission
  before provider capacity, with passing restart/cancellation and real controls.
  Normal hooks pass 1,479 Rust test executions; browser v3 adds 94 passing unit
  tests and 31 control/storage/profile scenarios. See the completion log.
  Deferred native mail ownership, Flutter checked recovery, admission before its
  Dart per-message wait, bounded restart projections and immediate Activity Undo
  remain active. Complete the other domain migrations and final
  timing/platform/shipping evidence as well.

  Integration `e84f66b` passes 117 Flutter Rust and 175 Flutter tests, including queued
  Send returning while delivery is held, pre-dispatch cancellation, conditional
  rollback, bounded Activity pages and visible recovery errors. Light/dark
  Activity captures are reviewed. Input-time lineage checks and Outbox retry/
  refresh are implemented; the eight focused Outbox tests also cover invalidating
  a review after its Sent phase changes. Native durable individual admission,
  physical lineage and the common background owner are integrated. Normal hooks
  pass 1,509 Rust test executions, with four ignored. Final native gates pass
  29 scenarios, with one live-mailbox skip, covering correlated close
  acknowledgements, calendar retries and cache repair while capacity is held.
  Independent accounts within one group now progress under one retained lease;
  21 integrated scheduler and 26 store tests pass; 24 native scenarios pass with
  one live-mailbox skip. Continuation `48cd150` is pushed with 1,516 normal-hook
  Rust executions passing, four ignored, and strict docs passing. The per-group
  storage-error cooldown now survives successful sibling completions and applies
  even when a failed receipt step advanced its claim; both focused recovery gates
  pass. Keep automatic retargeting over an unresolved legacy move explicit;
  the latter rejects admission until checked recovery proves its physical source.
  Indexed native 50-key candidate pages and a 100,000-row query-plan/VM-step gate
  are integrated for verification. The shared owner yields between pages and
  defers cursor resets. Per-lineage predecessor history and other domain query
  costs still need bounds; key discovery does not establish whole-app latency.
  Flutter receipt-before-cache persistence and exact dispatch baselines now pass
  126 Rust and 175 Flutter tests, including newer field ownership, replacement,
  cache-failure restart and missing destination UID recovery without replay.
  Schema 17 now persists the accepted field subset at claim, including partial
  supersession, receipt repair and exact Undo; 131 integrated Rust tests and
  all-target/all-feature Clippy pass.
  Fair Flutter resumption now uses indexed 50-row cursor pages, per-account
  exclusion and at most 32 requests; 132 integrated Rust and 182 Flutter tests
  pass, with three additional Activity visual scenarios passing.
  Typed refusals reject;
  pre-dispatch credential failure stays Waiting. Remote move plus flag payloads
  are rejected before admission until separate provider receipts are supported.

  Browser connection Activity ships in `48cd150` through its existing owner.
  Native schema 8 staged credential slots and checked adapters across sync,
  SMTP, vault, backup, import/removal and cleanup are integrated for verification.
  Preserve the previous connection until the new attempt activates. Local UI
  admission, bounded connection activity and queued-close controls remain in
  integrated verification, with isolated held-capacity/close/restart controls
  passing. Removal UI fencing is integrated. Incoming identity changes with cached mail need a
  reviewed migration; reject those changes until that migration exists.
  Combined native verification initially passed 26 of 27 scenarios; the rejected
  synced password scenario found staged slots awaiting cleanup. Vault completion
  now uses the existing cleanup owner, retaining durable failed-cleanup recovery.
  The demo no-op is selected at adapter construction so injected keychain cleanup
  is exercised. All four rebuilt password/backup controls pass, including the
  formerly failing scenario and restart; all 27 combined scenarios are covered.
  Draft adoption is adding session-owned saving/failure feedback and Retry,
  preserving newer edits, parked drafts and forced-close saving through the
  existing revisioned persistence owner.
  Native draft recovery exposed Ctrl+, inserting a literal comma in the focused
  editor while opening Preferences. Preserve the failing `9aa5b298c215` fixture
  evidence. The command binding fix passes 31 composer unit tests and its saved
  native keyboard regression. Draft recovery and its final compact contrast
  rerun pass; light/dark captures are reviewed and included in `48cd150`.
  Native preferences already preserve field intent and save through the local
  writer, but failures only use the shared notice. Add revision-owned save status
  that survives unrelated notices and ignores stale failure replies; retain local
  changes and explicit Retry without reverting successful local preferences when
  later profile publication fails.
  The preference-owned error/retry adapter is implemented with stale-result and
  close regressions; native desktop/compact failure, navigation and restart gates
  pass on the initial native build with reviewed desktop/compact captures.
  The combined rebuild passes the preference native gate. Flutter resumption
  fairness v3 fixes page-read error loops, disposal, active-account retry and
  cursor-reset starvation; its real Activity Retry and indexed seek are integrated.
  Flutter preference saves retain local values and field ownership, but their
  failure currently shares the unrelated mail notice. Add owned save status and
  visible Retry in Preferences, with delayed-save and unrelated-error controls.
  This adapter and retained Flutter/browser draft sessions are now integrated:
  196 Flutter tests, 132 Flutter Rust tests, 51 browser unit/provider tests and
  11 Chromium draft/Forward/queued-Send scenarios pass. Compact preference and
  draft captures are reviewed. Browser schema 15 concurrent-editor review,
  independent preference Retry and Flutter schema 18 account lifecycle are now
  integrated for the next checkpoint. Browser has 113 focused passing tests and
  37 distinct passing control/storage scenarios across bounded reruns. Its
  100,000-row case passes unchanged in a quiet window after loaded-run timeouts.
  Flutter passes 134 Rust and 203 Flutter tests, with two additional reviewed
  recovery goldens and 26 final FFI/control regressions. Keep exact draft observations,
  same-attempt retry generations, shared reconciled duplicate results,
  removed-account fences and the 32-attempt bound. Checkpoint `2605ff5` is pushed
  to main after normal hooks and strict docs; the full architecture remains open.
  Browser folder gateway/admission and shared Flutter calendar adoption are
  continuing in isolated lanes; no fixture-only provider parity is accepted.
  Native schema 9 folder creation and Preparing Outbox admission are integrated,
  with exact identity, cancellation, attachment and import fences. The combined
  library gate passes 1,189 tests with two ignored; combined native controls and
  normal shipping gates remain. Browser schema 16 folder creation and its real
  gateway are integrated: 75 focused units, four Chromium controls and 49 backend
  tests pass, with one ignored fixture prerequisite. Per-candidate domain fairness
  adds two regressions and is under final verification. Checked browser folder
  Move/Delete/Rename, Flutter Google calendar integration and CalDAV continue in
  isolated lanes. Native account removal also still
  waits for provider/account ownership; migrate its admission separately after
  folder creation, keeping reviewed local deletion and late-result fences.
  Flutter Google calendar schema 19 passes its initial 141 Rust/210 Flutter gates,
  but integration review found real Delete activity decoding, source-scoped event
  identity, empty-cache retirement and newer-intent projection gaps. Correct these
  with actual FFI and workspace regressions before shipping that adapter. Passing
  fixture tests alone do not establish its delete/restart behaviour.
  Three owned HTTP contract tests now verify Google create identities, ETags,
  retry/refusal policy and lost replies. Fence a stale sync listing against newer
  acknowledged calendar writes before shipping; sync and mutation currently use
  different operation locks.
  Real calendar editing must retain ETag/remote identity and event fields. The
  initial editor reconstruction dropped them and could turn an edit into CREATE;
  enforce immutable edit identity in Rust and cover the actual editor controls.
  Known readonly sources need backend admission/claim validation as well.
  Save receipt repair must accept its exact previous cache version while refusing
  a newer ETag, even when content matches. The initial adapter rejected ordinary
  edits and could replace same-content newer versions; keep both regressions.
  Google correction v2 is integrated for verification. Remaining release gates
  include a coherent read transaction, stale UI/sync reply fences, cache repair
  with occupied provider slots, actual FFI Delete/reopen, and durable Google
  subject binding so reconnect cannot send queued work to another account.
  Checked browser folder recovery must let an acknowledged action with replaced
  local rows stop tracking after a fresh review, retaining receipts and cached
  mail. Never leave the account permanently blocked or treat that choice as
  provider success. Freeze the provider/cache observation and reject stale reviews.
  Native schema 10 removal and Flutter schema 20 Google subject binding are now
  integrated for verification. Removal hides only after durable admission and
  retains failed cleanup through restart. Calendar snapshots own their cached
  subject; admission stays local/offline, while dispatch requires the matching SDK
  grant. Keep coherent snapshot, stale sync, provider-capacity and migration gates.
  Native removal now passes 1,204 combined library checks, 47 integration checks
  and ten distinct real native scenarios after fixing test navigation readiness.
  Browser schema 17 checked subtree changes are integrated: 235 unit, 57 backend
  and 22 Chromium scenarios pass, including outgoing/storage regressions;
  the production build and backend Clippy pass.
  Flutter schema 21 adds exact saved admission requests and readonly lost-reply
  lookup; 154 Rust tests pass. Dart request retention/reconciliation now passes
  29 integrated controls/FFI tests, including bounded unknown requests and
  coherent close recovery. Shared Google HTTP tests pass eight cases; its new
  authenticated gateway and shared Drive/Calendar grant owner pass 67 backend
  tests. The full Flutter suite passes 234; after shared HTTP extraction, its
  Rust suite passes 153. CalDAV now targets schema 22, not 20.
  Final native ten-of-100,000 pixel measurement passes 20 cycles: review p95
  32.31 ms and confirmation p95 22.14 ms, both against 100 ms. Separate correctness
  keeps full database comparison enabled. The first timing run was contended by
  that optional test observer; preserve its failed report and the query-step
  diagnosis. Agent builds/tests were stopped; ordinary user applications remained
  running. Common performance-gate wiring and its tests pass. Checkpoint `8db87f1`
  passes normal hooks with 1,586 Rust executions, ten ignored, and ships with the
  completion evidence update. Continue remaining client/domain and platform work;
  this does not complete the app-wide request.

Sam reports new mail is slow to arrive and moves failing on `sam@shep.so`
(Stalwart at `mail.shep.so`). These items outrank everything below. Sam's
14 September direction supersedes the "do not start a new synchronisation
design" note in the cross-device discussion item for the parts listed here.
Plan before implementing; nothing below has started.

**14 September related-messages fix:** the "Could not load related messages:
Query returned no rows" notice after archiving the open email is fixed and
pushed as `3dabf16`, with a regression test. Completion records the evidence.

The Flutter and browser clients have no grouped conversation view (only
"Search conversations" and reply quoting), so no client path can fail the same
way; conversation grouping itself remains an open client parity gap.

**14 September delivered:** missing destination discovery and special-use
resolution are pushed as `9039d70` (lane `9a27be1`); small-first download
lanes as `fe9be95` (lane `7afc164`). Completion records tests and limits.
Both still need Sam to confirm on the live account: archive and delete on
`sam@shep.so` should now land in a created `Archive` and in `Deleted Items`.

- [ ] **14 September destination follow-ups:** desktop done on `fix/move-destination-followups` (22 September): move toasts name the folder the receipt reports (account-specific decoded label, falling back to the requested name when receipts disagree); NAMESPACE (RFC 2342) is exchanged on the raw stream as the last discovery fallback on servers advertising it, with loopback tests for an empty reference listing and tagged NO/BAD/partial replies; a logical Archive/Trash/Junk move before the first folder sync lists the server's folders first and falls back to the literal name if that listing fails (Sam's decision; nothing is cached, so the next move lists again). Dragging onto the unified Archive/Trash/Spam rows also accepts an account whose folder exists only under its special-use name (it was rejected as unavailable). Still open: Flutter/browser moves need the same discovery order, special-use resolution and acknowledged-folder toast; no live server without a `LIST "" ""` root has been tried; Archive and Delete toasts keep "Archived"/"Deleted" without naming the physical folder (Sam's decision).
- [ ] **14 September local fallback follow-ups:** device-only completion of refused moves is merged as `b70712b` (lane `c6228d8`), with the user note that other devices keep the old folder until the server accepts. Still open: flag changes on a device-only row wait for the server move; a source deleted on another client leaves the row device-only until manual review; cross-account transfers never fall back locally (needs the cross-device discussion); the retry cadence (three records, ten minutes) is a first choice; Flutter/browser need the same behaviour.
**15 September migrated check interval:** database version 5 moves a saved 15 second interval to 5 once at open; other values and a later deliberate 15 are kept. The check interval is not a shared-profile setting, so nothing syncs. Completion records the test.
- [ ] **14 September per-account sync follow-ups:** the 5 second default and independent per-account checks are merged as `0431962` (lane `c9e4a97`). Still open: the account listing and per-account workspace republish add load with many accounts; Flutter/browser clients keep their own cadence and need the same per-account model.
- [ ] **14 September slow-item follow-ups:** the two-lane download order is shipped (`fe9be95`). Still open: ordering is per 50-UID metadata chunk, so a small message in an older chunk waits for the newer chunk; new arrivals do not show until their body lands (headers-only placeholders would need a store change); CONDSTORE `CHANGEDSINCE` flag refresh needs a persisted per-folder HIGHESTMODSEQ; the 1 MiB threshold is unmeasured against the live server.
- [ ] **14 September server push follow-ups:** IDLE watching of Inbox per IMAP account is merged as `294be90` (lane `dd19a2f`); Sam should confirm on the live account that new mail appears within a few seconds. Still open: CONDSTORE/QRESYNC so a pushed check fetches only changes (needs a persisted per-folder HIGHESTMODSEQ and VANISHED handling); NOTIFY for multi-folder push (imap-proto lacks a parser); a watcher does not pick up edited host/port/username until the account is removed and re-added; Flutter/browser clients still poll.
- [ ] **14 September taskbar badge follow-ups:** the acknowledgement drift is fixed and merged as `e4dcf5b` (lane `93c1656`); Sam should confirm on the installed app once released. Still open: a server flag listing that lands before a local read reaches the server can raise the count again until the next check (sync-side ordering); a dropped requery when the query lane is full delays other store changes until the next sync; no real dock render evidence on any platform.
- [ ] **14 September non-interrupting actions follow-ups:** mail actions beside a live download are merged as `5dad5cb` (lane `877c414`). Still open: exclusive lifecycle work (account save/test, removal, restore, import, folder creation) still interrupts a download; a flag entry shields both read and flagged state for one cycle; the grace window equals the 600 second cycle bound; Sam should confirm on the live account that archiving during a check no longer pauses new mail.

**18 September sidebar rows:** the eight failing native move flows were stale click coordinates, not a product fault: `12e6e2d` added the unified Spam row, moving every sidebar row beneath Trash down one row and the Move dialog rows up. `885ac43` (lane `52e36b8`) re-points 23 scenarios, pins the unified row order in a unit test and makes `--functional-only` honour `-k`; the deletion-failure scenario now expects the move-failure wording. Completion records the counts.

## Desktop quality CI is red on both platforms

Reproduced on clean runs of `3dabf16`, `9039d70` and `a3654ed`; documentation CI is green since `3d7ebce`. Website CI is blocked on the runner's corrupt BuildKit cache ([infrastructure#3](https://github.com/sam-ruff/infrastructure/issues/3)).

- [ ] **25 September HTML pixel budgets on the runner:** on #13's run 35901210235 every other gate passed, but HTML opening p95s were 3-4x over budget (warm return 145 ms, cold nested table 213 ms) while this workstation measured 26-60 ms on the same revision. **Owner decision, 25 September:** CI runs `performance_gate.py --html-report-only`, which still fails on missing, invalid or undersampled HTML evidence but only reports the budgets; `scripts/check.sh` stays strict. The action-latency setup now waits `LARGE_SELECTION_SECONDS` for the 100,000-message Shift-range rebase (about 1.1 s here, over 3 s on the runner), like the native suite. **Remaining:** confirm the Linux job goes green, and restore strict HTML budgets on CI if the runner gets quiet or dedicated capacity.
- [ ] **23 September Windows CI paused:** every Windows job since 22 September lost contact with GitHub about 12 minutes in (runners `ci-runner-200` to `ci-runner-204`), before or during the Rust gates, while several Linux jobs shared sophie. Sam approved pausing it: the Windows matrix entry in `ci.yml` is commented out and `release.yml` publishing is paused so no Linux-only release ships. The installer/harness test fixes are in [#9](https://github.com/sam-ruff/shep.so/pull/9); the runner-pool root cause (ci-hatchery lease/health timeout or memory reserve) is being diagnosed read-only. Restore the matrix entry and release publishing together once a Windows run passes.

- [ ] **15 September responsiveness benchmark on the sophie runner:** the three relevance search cases sat at or over the 50 ms budget on the runner (multi-term p95 60 to 134 ms, transposed 51.6 ms, FTS 45 to 50 ms). Single-pass tiered ranking (`e585cdb`, lane `92aeea3`) fixed that with about 150 lines of unsafe FTS5 FFI. **Owner decision, 15 September:** Sam chose the safe and correct query over the FFI and asked for the search budgets to be raised to match it. Done: `e585cdb` is reverted and the search budgets are 100 ms (one or two terms) and 150 ms (four terms), page budgets unchanged. **23 September:** with Sam's permission to raise budgets slightly, the one-or-two-term budget is 125 ms: two runner p95s on #13 were 107.55 and 102.07 ms (transposed; FTS 96.98 ms) with runner p50 about 50 ms, against about 31 ms p95 locally at low load, so the runner is slower and spikier rather than regressed. Four terms stay at 150 ms. Confirm the runner goes green. The measured safe wins (`PRAGMA cache_size`, FTS `optimize` after bulk loads) stay available if search feels slow.
- [ ] **23 September inbox page benchmark regression:** from `8db87f1` every page, count and launcher badge query ran a correlated account-removal check per message, so the 100,000-message Inbox page took p95 703 ms on the runner (budget 50 ms). Fixed on `fix/ci-inbox-page-benchmark`: only removed accounts that still own cached mail are filtered, as a bound list, and projected views use uncorrelated subqueries; a query-plan test guards the covering count and the benchmark adds an Inbox page with a removed account. Local p95 11 ms, 17 ms with a removed account; on the runner (PR #10) p95 22.9 ms and 15.6 ms, with every benchmark case passing. **Remaining:** merge, then the Linux job goes green once the native-suite fixes (#11) land. `performance-budgets.json` now matches Sam's 15 September decision (R103, `0a786e3`): search 100 ms, transposed 100 ms, four terms 150 ms; a gate test compares it with the benchmark's assertions.
- [ ] **18 September Windows test failures:** with the manifest in place (`ca6163a`) the Windows job ran its tests for the first time on `dac5dfb`: 850 passed, 26 failed, 2 ignored. Failures by area: nine `backup::ftp` wire tests plus `engine::backups_tests::ftp_verified_connection_stores_keys_only_after_success_and_keeps_later_settings` (the in-process FTP test server, `src/backup/ftp_test_server.rs:71`, panics on Windows, probably socket or path handling); fourteen `cache_cipher::{bootstrap,publication}` tests (data-root ownership, plaintext replacement and recovery rely on POSIX rename, lock and delete-while-open semantics that Windows refuses); `engine::dispatch::tests::cached_reads_and_ordered_saves_complete_with_all_network_jobs_and_queue_occupied`; `profiles::tests::legacy_cache_and_credentials_are_preserved_until_an_explicit_switch`. Fixed from the CI panics in `1dcaf98` (ECDSA test certificate Schannel cannot load, checkpoint flush through a read-only handle, two canonical-path comparisons); Linux suite and the GNU cross check are green. Confirm on the next Windows job; each test failed at its first Windows-specific point, so a later masked difference may still appear.
- [ ] **Native suite on the runner (first full run `834be5f`, baseline fixed 23 September):** `fix/ci-native-baseline` fixes every native failure reproduced on `9dd7548` in the CI image (32) and on this host: stale sidebar, Preferences and composer coordinates after the Activity entry and draft status line; the Reading and layout "badge" synonym (same change as PR #7) and the ninth portable setting; individual-action expectations that predate the durable journal; three product faults (superseded queued jobs never reached the window, a refused durable move showed no device-only notice, and a late layout focus request could steal a click); a bulk scheduler that rescanned every finished domain on each wake, making 120-message archives about twice as slow; and test races. The GNOME scenarios now run in the CI image with an owned system bus. **Remaining:** confirm on the runner. The two kiosk PDF print scenarios skip where the browser cannot kiosk-print a trivial page to PDF (Chrome for Testing 153 in the CI image, reproduced without Shep): decide whether the image gets a browser whose kiosk printing works. The two 120-message archive flows and the 100,000-message selection wait now allow measured background time (`GROUP_ARCHIVE_SECONDS` 30, `LARGE_SELECTION_SECONDS` 20 in `scripts/e2e.py`); their immediate-feedback checks are unchanged. Follow-up: each archive step still costs several store round trips beyond its receipt transaction (a calendar retry query and an Activity refresh per update, plus job and item re-reads); coalescing those would cut the runner's 0.2 to 0.5 s per step. On this Ubuntu 26.04 host only, F-key captures arrive as Alt+F-key and text-menu clipboard checks fail through the scratch X11 tools, while the same scenarios pass in the CI image.
- [ ] **18 September Windows installer tests on the runner:** with Rust tests green on Windows, `tests/test_windows_installer.py` fails 10 and errors 4 because every run throws "No release could be downloaded" at `install-release-windows.ps1:227`: the test's `Receive-ShepDownload` stub fetching from the local fixture server fails on the runner and the installer's `catch` hid why. The message now carries the inner exception; read it on the next run and fix the stub or the runner (no proxy variables are set on that job).
- [ ] **18 September Linux job cap:** the Linux quality job now runs unit tests, the benchmark and the whole native suite in sequence and was cancelled at the 120 minute cap on `dac5dfb` with the suite 36 minutes in. `timeout-minutes` is raised to 240; if the suite keeps growing, split the native suite into its own job so a red benchmark and a red flow are reported separately.
- [ ] **15 September Windows test binary fails to start:** `cargo test --locked --all-features` on the Windows runner dies at process start with `STATUS_ENTRYPOINT_NOT_FOUND` (0xc0000139) for the `shep` lib test binary on every run so far, so no Windows test has executed. **Cause found 15 September** by the CI import diagnostic: the only absent import is `comctl32.dll!GetWindowSubclass`, used by `src/desktop_badge/windows.rs` (with `SetWindowSubclass`, `RemoveWindowSubclass`, `DefSubclassProc`). Those are exported by name only from Common Controls v6, which a process gets only when its manifest declares that dependency; no binary here embeds a manifest, so the loader binds the v5 `comctl32` and the process cannot start. This affects the release `shep.exe` on Windows as well as every test binary. Fixed in `ca6163a`: `build.rs` embeds `assets/windows/shep.manifest` (Common Controls 6.0.0.0) through `embed-resource` into every binary, tests and benches included, leaving the crt-static `RUSTFLAGS` untouched. Confirmed on `dac5dfb`: the Windows job now executes its tests (850 pass; the 26 failures are the separate item above). Still open: confirm an installed Windows release starts and shows the taskbar badge.

## Highest priority - other accounts' folders in the Move dialog

- [ ] **18 September foreign folders when moving (R108) follow-ups:** the desktop feature is merged as `bd15369` (lane `7a29b44`, `7a5b884`): foreign folders appear only while typing, 15 point penalty, account badge, Enter/Escape confirmation, preference synced as `foreign_move_folders`. Open: Flutter and web decode and label the key but their Move flows do not act on it; the Flutter bridge test over the new codec fixtures was not run; Sam to confirm the feel on the installed build.
- [ ] **18 September sync every preference:** Sam's rule is that every preference syncs through the shared profile unless profile sync itself is turned off. Audit the `Preferences` fields absent from `portable!` (`src/preference_edits.rs`) and the codec `SettingKey` list (`shortcut_tooltips`, `close_to_tray`, `mail_check_seconds`, notification and backup settings, palette and layout values, and the rest), decide which are user settings rather than device state, and add them to the codec across desktop, Flutter and web in one coordinated change.

## Highest priority - stale running instance after an install

**18 September delivered:** a launch from a newer binary now restarts the running owner through the local socket and takes over, or shows a plain quit-and-reopen notice when the owner cannot answer (`b07484e`); the installer prints the same advice. Sam's current process predates the identity, so this one time he must quit it from the tray himself; from this build on the handover is automatic.

- [ ] **18 September settings-search native regression:** `test_profile_sync_native_invalid_local_state_keeps_close_and_navigation_available` fails on `main` because the settings search for "shared profile" now returns two results (`Profiles and sync`, `Profiles`) after the 11 September catalogue change, while the 9 September scenario expects one. Decide whether the catalogue or the scenario is right and fix the other; do not weaken the scenario.

## Highest priority - extended move and undo e2e flows

- [ ] **18 September rapid move, undo and continue flows:** the fixture family (`8a2231c`) and the live family against `e2e@shep.so` (`7061394`) both pass with no desync, artefact or badge drift; the one product fix was the page left a row short after a move until the receipt arrived. Sam's report predates that fix and today's sync changes, so the next step is a release for Sam to try. If the symptom persists on the installed build, the remaining unmodelled paths are a large real mailbox with many pages and slow bodies, and Sam's own folder set; extend the live seed to several hundred messages with attachments and rerun.

- [ ] **18 September live e2e mailbox:** Sam approved a dedicated `e2e` account on the shep.so Stalwart server, declared in the infrastructure repository's `mail_server_accounts` with its vault password (the vault is available to agents on this machine), for a gated live-server variant of the rapid move and undo flows and for verifying the shipped sync changes (Archive creation, `Deleted Items`, IDLE arrival, badge) without touching Sam's mailbox. Provisioned 18 September: `mail_server_accounts` gained `e2e`, `vault_mail_password_e2e` is in the vault, the role applied (changed=4, repeat changed=0) and an IMAP login as `e2e@shep.so` succeeds with IDLE advertised. Delivered in `7061394`: the gated live mode, the seeding script and five `test_live_*` scenarios; Archive creation, `Deleted Items`, IDLE arrival within 2 s and the badge are verified on the real server. Open: the infrastructure commit `e091493` is local only; a second live mailbox would allow cross-account live flows.

## Top priority - OAuth and shared profiles

- [ ] **13 September release CI and installers:** enable and verify release CI so the install scripts reliably use published, versioned platform assets and checksums. Use only the authorised self-hosted runner pools, inspect actual platform capacity before enabling jobs, preserve installer failure/upgrade contracts, and verify the resulting release and install path. This request authorises enabling the previously disabled quality/release workflows; never fall back to GitHub-hosted runners.

**13 September desktop delivery:** `12e6e2d` is pushed and its production build
installed. Recovered originals accept fresh Move/Delete, HTML uses library-backed
selection, titles are selectable, ordinary-cache receiving stages large messages
without the skipped-size bar, and Spam opens Junk beside Trash. Normal hooks
pass 1,305 Rust test executions; 51 distinct native scenarios and the final
selection rerun pass. Completion records the binary hash, evidence and limits.
Running copies need normal Quit/reopen. Wider R23 and client gaps remain below.

- [ ] **13 September client and folder-role parity:** verify Flutter/browser equivalents for the recovered-original action fix, standard HTML/subject selection, large-message receipt and Spam controls. Desktop's initial Spam shortcut targets the reported server's `Junk` folder; discover other providers' special-use aliases without guessing or renaming physical folders.

- [ ] **13 September cross-device mail/folder discussion:** following the installed desktop fixes, discuss shared email/folder state with Sam, including read/flag state, moves/deletes, physical folder identities, offline conflicts and which Shep-only state should follow the user. Do not start implementing a new synchronisation design before this discussion.

- [ ] **12 September real Google login configuration:** guide the owner through creating or reusing Shep's Google Cloud project and Desktop OAuth client, then verify a production login build. **Owner correction:** store the public Desktop client ID and accompanying installed-app client secret in this repository and embed them in the app. Personal access/refresh tokens stay in secure credential storage; confidential future web-server credentials belong in the infrastructure vault. Preserve the same-project requirement for future clients. Client configuration has not been supplied yet.

- [ ] **12 September text-menu parity:** verify Flutter/browser equivalents for editable fields, selectable readers, password restrictions, clipboard/focus ownership and keyboard context-menu access; retain platform-native controls where they fulfil the shared contract.

**12 September current execution order:** completed search, tray, notification
and icon work is already on main through `0abec9f`. The moving-mail fix is
committed as `b614b8d`: missing destinations are created before submission,
provable recovery completes automatically, and direct Retry avoids a mandatory
review. Nineteen native scenarios and 1,263 hook test executions pass; completion
records the source and evidence. Sidebar New folder is pushed as `842c570`, with
thirteen native scenarios and 1,277 normal-hook executions passing. Standard text
menus are pushed as `bc9ca09`, with 27 distinct native scenarios and 1,287 hook
executions passing. README installer fixes are pushed as `c3296ce`, with 124
Python tests (seven unavailable Windows skips), strict docs and 1,287 hook
executions passing. The verified production binary and approved launcher/icons
are installed; existing processes require normal Quit/reopen. Completion records
the exact binary hash and platform/configuration limits. This requested desktop
sequence is delivered; the wider TODO remains active.
Flutter/browser missing-folder creation, sidebar creation controls and automatic
proof-based move recovery remain explicit active parity gaps.
Automatic creation for other provider write paths, including Sent-copy
publication, still needs a separate receipt and recovery review.

- [ ] **12 September folder creation parity:** desktop missing Move/Archive destinations and sidebar **New folder** are shipped as `b614b8d` and `842c570`. Implement Flutter/browser creation controls and equivalent durable retry, hierarchy/encoding and close behaviour; live provider verification remains open.

**12 September GNOME notifications and icons follow-up:** the sender-lifetime
fix is committed as `ae85450` and the production build is installed with the
approved launcher/tray icons. Integrated detailed, private and muted GNOME
notifications and taskbar restoration pass with reviewed native evidence;
1,226 normal-hook Rust test executions pass. Completion records the shipping
receipt. Live-provider, sound, other-platform and client parity remain under R82.

**12 September decision and tray regression:** Abbreviations is selected and
pushed on main as `86d5ca1`, with twelve native scenarios and 1,202 normal-hook
test executions passing. Precise and Tolerant branch references are deleted
locally and remotely; their comparison evidence is retained. Duplicate-launch
ownership and GNOME dock/minimise restoration are pushed as `7ef9d74` and
installed with the current launcher/tray icons. All thirteen existing tray
native scenarios and the real GNOME launcher fixture pass, alongside 1,216
normal-hook Rust test executions. Older running copies require a normal Quit
before using the new launch protocol. The notification fix is recorded above.
Actual Windows/macOS and Wayland launcher/restore execution must remain an
explicit platform gap after Linux/X11 verification; browser tabs keep their
existing independent ownership model.

**12 September live search comparison:** Sam requests launching the three search
branches in turn against his actual profiles, replacing the earlier fictional
workspace launch for this review. Start Precise, then Tolerant, then
Abbreviations, allowing Sam to test each before switching. Use the existing
profile selection and credentials; do not replace the installed executable.
Sam's first feedback: Precise (`4b14a39`) was "quite good". Tolerant (`1c43ec1`)
has been tried; Abbreviations (`b00c784`) is now open and focused against
the normal profile catalogue and active mail cache. These personal-profile
trials are not automated fixture evidence. Sam subsequently selected
Abbreviations, as recorded above.

**11 September search-demo priority:** Sam requests working through this list and pushing each fully verified feature as it finishes. First deliver R95 formatted HTML selection, R35 newest-first reply conversations, R96 pinned reply actions and R64 launcher/tray icon fixes. R35 already records the requested message reordering; do not conflate it with R96. Then prepare three separately pushed demo branches comparing fast, relevance-ranked algorithm combinations for Preferences/options search (R99) and general mail/Move search (R18/R44), with strongest text matches first. Keep the alternatives separate for Sam to demo before choosing a default; this order supersedes the earlier OAuth-first order for this work. Preserve client parity tracking and benchmark each alternative on realistic mailbox sizes.

Flutter and desktop have durable discovery, reviewed publication and account/preferences enrollment with independent histories. The desktop implementation is main's (`src/profile_sync`, `src/store/profile_sync`); the client branch's own desktop sync and checked, paged conflict decisions pushed in [`28c2884`](https://github.com/sam-ruff/shep.so/commit/28c288448842b7d09543fc28ef1f2f14bf36f142) were superseded by main at the merge, and their validation and earlier checkpoints remain in [completion](docs/COMPLETION.md).

**Current work:** ongoing Flutter preference reconciliation is connected (10 September, see the completion log): a durable native subscription/edit/application ledger over the enrollment history, a bounded foreground cycle that admits local edits before pulls, applies safe remote values through one device request at a time and defers conflicts, per-preference controls, Sync now, a paged Keep mine / Use profile conflict review and an explicit paused state when Google Drive is disconnected. Only the eight portable preferences reconcile; subscriptions seed only from a completed enrollment; scheduling is the foreground 15-second tick.

Next add OS background scheduling, subscriptions seeded from a first publication, account definitions/removals and other categories to Flutter reconciliation. Then finish automatic restoration/first setup, account lifecycle, all portable settings/categories and authenticated cross-client interchange. Preserve tracked upload identities, reconnect guards and explicit credential review. Incomplete discovery or a different Google project's app-data space must never imply empty setup.

- [ ] **R92 / R75 (client) / R02 / R49 — Implement OAuth profile sync:** implement Google OAuth and cross-device account/profile sync as the top priority, following [the interoperability handover](docs/agents/PROFILE_SYNC_HANDOVER.md). Cover first setup originating on Rust desktop or Flutter, discovering existing profiles on new devices, continuing sync on enrolled devices, profile/category toggles, offline edits/conflicts, account removal and secure credential import. Full database export/import and local profiles (R83) are shipped in `93d4289`; coordinate complete database transfer with them. All profile/sync choices must be configurable and toggleable. The inspected integration points and proposed shared-format/OAuth/credential/enrollment/testing contract are shipped as `02c4b32`, with audit `59578f3`; use the current shared profile-core format and client consent work rather than rewriting the handover. Keep this first until delivery. Shared codec/history, scoped desktop/Flutter consent and durable Drive discovery are implemented prerequisites; their tests and commits remain in [the completion log](docs/COMPLETION.md). Browser server-mediated consent, discovery, reviewed publication, reviewed enrollment and first-setup onboarding landed 10 September over the shared profile-core WASM history (see the completion log); ongoing Flutter preference reconciliation landed the same day. **Remaining:** ongoing browser reconciliation and conflict decisions, shared removal reviews, automatic desktop setup, all portable settings/categories (the browser carries four), OS background scheduling, automatic native SDK restoration, safe account switching, a live production Google provider for the browser backend and verified same-project cross-client access. Preserve the active grant after denied consent and require reviewed switching where the SDK supports only one account. Keep account passwords out of unprotected exports/sync files; Sam chose **Google-only protection** for synced account passwords on 11 September 2026, and the desktop's opt-in password vault is integrated as `835cfb6` (see R49). Passing fixtures, discovery or a pushed handover do not establish working continuous sync or credential portability.

- [ ] **R75 (client) — Sign in with Google:** replace manual Google token entry with an OAuth **“Sign in with Google”** flow across desktop, Flutter and browser clients. **Sam, 11 September 2026:** the desktop still makes users create and paste a Google Cloud OAuth client ID and secret; replace that with a single **Sign in with Google** button. Shep ships its own installed-app OAuth client (loopback redirect with PKCE), so users never create or paste OAuth credentials; any developer override stays out of the normal UI. Shep's own Desktop OAuth client must come from Sam's Google Cloud project. Audit existing Google setup screens and distinguish application OAuth configuration from user access/refresh tokens. Request only the scopes needed by enabled features, keep secure token refresh/revocation and account switching, and test denied/cancelled/expired grants. The browser beta gate alone does not connect Gmail, Calendar or Drive; keep provider access and live verification explicit. **Desktop (11 September, `ff04df5`, integrated as `a75cc4d`):** Preferences now shows one **Sign in with Google** button and no client fields. Shep's Desktop client is compiled in from `SHEP_GOOGLE_CLIENT_ID`/`SHEP_GOOGLE_CLIENT_SECRET` (runtime override only in debug/test builds); a build without it shows the button disabled with "Google sign-in is not configured in this build." Sign-in keeps the system browser, random loopback port and scoped consent, and adds a fresh PKCE S256 verifier, exact state, a three-minute bound with **Cancel sign-in**, and clear denied/cancelled/timed-out/expired errors. Grants from a self-configured client keep refreshing until the next sign-in. See [Google sign-in client](docs/agents/google-sign-in.md) and the completion log. **Remaining:** Sam must create Shep's Google Cloud project, OAuth consent screen and **Desktop app** client, enable the Drive and Calendar APIs, add test users, set the repository variable/secret for builds and complete Google's verification for the Calendar scopes; live sign-in with that client is unverified. Flutter and browser sign-in belong to the mobile session.

- [ ] **R02/R49 — Continuous Drive sync:** OAuth/appDataFolder account definitions and portable preferences, first-device opt-in, existing-setup discovery on another PC, ongoing toggle, conflict-safe changes and offline recovery. Sync appearance, shortcuts, contacts and other portable settings; keep local paths/window placement device-specific.
  Native creation/recovery, reviewed import, eight portable settings and post-login discovery/enrollment are shipped in `acb4969`. One complete profile imports automatically into an untouched workspace; Not now persists opt-out and populated devices retain review. Common-value/local-edit checkpoints are durable. Ongoing publication/application is shipped in `bd50c52`; conservative reviews and further interoperability work remain below. See [completion](docs/COMPLETION.md) for the 30 native scenarios and shipping evidence.
  The current cycle admits local edits before pulls, applies safe remote preferences/account names/new definitions, and preserves deferred conflicts plus dirty native preference fields. Four new native paths cover automatic receipt, local publication, offline retry and partial failure; they pass on the integrated `d29ce06` binary.
  Durable native edit generations now preserve setting/account-name reversions through pulls, acknowledgments and restart. Next review work is in `codex/profile-account-reviews`: native connection-field reversion tracking passes 102 profile tests and 769 lane hook executions; integrated and pushed as `ff03b02` with 816 hook executions and 18 selected native scenarios. Next add explicit shared endpoint/removal decisions with lifecycle checks, reconnect gating and local-mail preservation; require protocol/restart/native evidence before shipping. Next finish incremental polling, already-admitted legacy recovery, password-prompted legacy backup migration and remaining portable settings. Preserve field bases when edits race pulls and merge dirty UI preferences per field. Verify actual cross-client Google access; keep active client work intact.
  Verified immutable history caching is shipped in `e77eabc`, with 80 profile tests and nine integrated native scenarios passing. Incremental change-token pulls for enrolled devices are integrated as `ca28e69`: each cycle polls the shared catalog's persisted change token, copies only new verified records through a durable per-profile copy cursor bound to the observation and history device UUIDs, and falls back to one full listing per pass when Google rejects the token or a known file fails verification; setup and join keep the complete scoped listing. Eight targeted Rust tests, 120 matching profile tests, 73 shared-crate tests, 96 Python tests, 1116 hook executions and 18 selected native profile scenarios (including the new `existing-token-expired` fallback with reviewed captures) pass on the lane after merging `main` at `9ea8ad1`; integration and push remain with the primary agent.
  Portable-setting conflict reviews are implemented in `codex/profile-reviews`: bounded scalar choices, exact reviewed version sets, local/native generation checks and durable resolution requests. Six targeted store/history/controller regressions and 86 matching profile tests pass; 25 native profile flows pass with reviewed light/compact-dark/error feedback. The integrated binary also passes all 25 native profile flows plus 81 Python tests; source is pushed as `22a3af5` with 762 root hook executions passing. Existing endpoint/removal and account-link reviews remain separate unfinished work. Explicit import linking now preserves matching local account/mail/keychain identity, with unchanged-connection and stale-choice checks. All 100 profile tests pass (one personal diagnostic ignored); four native link/Add-new/compact/paging paths pass. Across all 30 native profile paths, one slow-upload completion timed out and the unchanged rerun passes; source is now pushed as `4e75005` with 798 hook executions and all 88 integrated native scenarios passing. Post-enrollment linking and suppression on populated devices are integrated as `924141d`: a new shared definition matching an unmapped native account by exact connection fields or address is held for review, with Link to existing account (exact matches only, keeping mail/credentials/identity and republishing no definition), Add as a new account and durable Keep this device's account local. Seven targeted Rust tests, 115 matching profile tests and three native `profile_account_link` scenarios (Link plus Keep on the address-only card, Add new across restart, compact-dark Keep local) pass with reviewed light and compact-dark captures. Remaining: global removal choices and live cross-client Google verification; synced passwords are tracked under R49.
- [ ] **R02/R49 — Shared removal reviews:** active `codex/profile-account-removals` adds explicit remote-removal review, durable Keep on this device suppression, and the existing local-data removal dialog. Eight targeted tests pass; all 98 profile tests, 84 Python tests and 35 final native profile scenarios pass, including Keep/Cancel/Remove/restart and stale-card disappearance. Normal hooks and main shipping remain pending.
- [ ] **R02/R49 — Shared endpoint reconnection:** connection tests and saves must refuse old keychain credentials while reconnection is pending. Guard checkpoint `442b45b` passes four command/credential/restart tests and is integrated and pushed as `13f9c36`, with 834 hooked executions and 13 native scenarios passing. Connection review checkpoint `18413e7` preserves the previous account/mail/credentials as local-only and adds reviewed endpoints with a fresh reconnecting identity. Six targeted tests, 96 matching profile tests, 84 Python tests, all 32 native profile scenarios and 779 lane hook executions pass. Integrated and pushed as `1c0fc45`, with 915 hook executions and complete native scenario coverage. The remote removal review checkpoint `ede75aa` (Keep on this device or Review removal for remote tombstones, durable local suppression, stale-choice rejection) is integrated on main as `61c6dfc` with 938 hook executions, 104 profile tests, 84 Python tests and five native removal/review scenarios passing; pushed in `c414227`. Light and compact-dark captures of the removal row itself and global account removal remain open; post-enrollment linking is covered under Continuous Drive sync.
- [ ] **R49 — Portable account credentials on new PCs:** implement secure import of synced account passwords into the OS keychain. **Sam decided on 11 September 2026: Google-only protection, no separate sync passphrase.** Anyone who can read the user's Drive app data can recover the synced passwords, so the UI and docs must say so and must not claim encryption against Drive. Follow the credential section of [the shared handover](docs/agents/PROFILE_SYNC_HANDOVER.md): documented key custody, a replaceable app-data vault outside the append-only history, secrets only in the OS keychain, staged incoming/SMTP pair activation after validation, and golden cross-language fixtures shared with Flutter. Do not claim password sync works until a client ships and verifies it.
  **Desktop, 11 September (integrated as `835cfb6`):** the handover's credential section now fixes the key custody, envelope, file layout, concurrency and device rules; `shared/profile-core`'s additive `vault` feature implements the codec with golden fixtures in `shared/credential-vault-fixtures.json` (checked by Rust and by an independent Python AES-GCM). Profiles and sync has an opt-in **Sync account passwords through your Google account** toggle with the Drive-app-data warning. It publishes each mapped account's incoming and separate SMTP password from the keychain, removes entries when an account is removed here or in the shared history (rotating the key) or when the toggle is turned off, and on another device stages, reads back and tests received pairs before activating them, so an imported Reconnect account works without retyping; a failed test keeps the previous pair and waits for **Try synced passwords again**. Evidence is in the completion log. **Remaining:** Flutter and browser implementations (mobile session; the shared codec compiles for native and WASM and the fixtures are ready); live Google verification of the app-data vault and cross-client access; atomic activation of the two desktop keychain slots (today SMTP then incoming, retried from staging on failure); and avoiding a download of the credential files on every sync check while the toggle is on (fetch only when the vault changed).

## Mobile, web and website (same monorepo)

- [ ] **Website migration hardening:** publish the tested full-SHA promotional image with OCI source identity, validate pull requests without registry publication, retain local-only CI, constrain its Docker context and revalidate unversioned assets. Infrastructure must verify the successful Website workflow and pin its image digest before staging and production promotion. Keep application release workflows disabled and record live deployment separately from source checks.

Client request numbers R67 to R80 in this section are the client session's own numbers.

- [ ] **R67 (client) — Flutter parity:** Keep Rust desktop at the root, Android/iOS in `flutter/`, the separate iced-style browser in `web/`, and promo in `website/`; the separate `shep.flutter` repository and hosted Flutter layout are superseded. Preserve the shadcn theme, approved logo, configurability and complete mail/calendar/provider/backup behavior. Shared MIME, drafts, attachments, Forward/Print, account removal, native credential handover, Sent identity and optimistic action foundations have recorded host/Android/browser evidence. **Remaining:** full composition/multiple editors, native/browser Sent labels/history, browser Outbox paging, bounded body/cache/large-MIME work, account editing with reviewed identity migration and browser credential handover, calendar/Google/backups, latest desktop defaults, native keymaps, complete lifecycle/accessibility/performance audit and Apple/live-provider execution. SQLite is still unencrypted. See the completion log and parity matrix; fixture success is not full parity.

- [ ] **R68 (client) — Mobile mail interactions:** K-9-inspired sender/subject/snippet rows, account/unread/attachment indicators and swipe action icons; independently configurable left/right swipes, immediate feedback, undo/failure recovery, accessible clickable equivalents and persistent preview settings. Continuation: shared acknowledged IMAP receipts/exact-content lookup, stable client identities, durable unresolved intents and queued/post-refresh Undo now have protocol/cache/browser/Android evidence. Finish explicit review/retry for ambiguous or unrecoverable moves, cross-account behavior and the complete accessibility/provider-success audit; do not treat this as full move parity.
- [ ] **R69 (client) — Flutter quality/release:** Continue Walkie Textie-style Playwright plus native integration/Appium testing. Preserve existing Android emulator, Flutter browser, formatted-reader, Forward, Print/PDF, account/credential and large-text control regressions. Check the generated HTML fixture against shared Rust preparation before both browser and Android preview builds. The Google Appium flow now selects clickable controls, scrolls through actual touch input and verifies cancellation/retry/disconnect; all seven flows pass, with failed screens/trees retained in the completion log. **Remaining:** Apple simulator execution, comprehensive happy/error/recovery/lifecycle/performance coverage, signed coordinated desktop/mobile/browser releases and distribution. Keep quality/release CI disabled; documentation CI remains enabled. Three profile-sync Dart test files from an earlier lane are not formatted to the pinned Dart 3.12.2, so the (disabled) CI format check would fail on them; format them before enabling client CI.

- [ ] **R70 (client) — Promo website:** Delegated agent source/assets are integrated into the combined review branch: concise copy, synthetic screenshots, user-agent-aware primary installation, all-platform alternatives and honest Android/App Store/native/browser availability. On 11 September the site gained a Chainguard nginx image and a website-only `Website` workflow, and its availability copy was re-audited against main (see R94 and the completion log). **Remaining:** deploy through R94, route `/beta` to the protected browser client once it is live, and replace unavailable destinations with verified store/release links as distribution becomes available. Do not advertise unpublished clients as installable.

- [ ] **R71 (client) — Browser hosting:** build a separate browser client in `web/` matching Rust iced and deploy at shep.so, also supporting network access. Correction R74: a Rust backend on the email VPS is now authorized for beta verification and IMAP/POP3/SMTP transport; the static-only/local-bridge proposal is superseded. Retain client/provider ownership and no persistent server-side mail/password storage unless explicitly changed. Keep current development in worktrees; obtain VPS/OAuth configuration before deployment. Do not claim browser TCP sockets or preview behavior establish provider parity.
- [ ] **R72 (client) — Linux app stores:** investigate and prepare Flathub and other appropriate Linux distribution/store submissions, packaging, metadata, screenshots and verification; track publication separately.

- [ ] **R73 (client) — Continuous parity:** The AGENTS reminder, docs/CLIENT_PARITY.md, shared scenario matrix and parity checker are implemented. Keep them current for every desktop feature/change: implement equivalent mobile/browser behavior or retain an explicit active gap with tests. Browser behavior follows Rust iced; mobile uses the Flutter layout. Desktop changes now arrive through `main`, the single integration branch; review each merge from `main` for new parity gaps instead of porting individual commits.

- [ ] **R80 (client) — Visual parity with the desktop app (requested 9 September 2026):** make sure, visually, that the Flutter app and the browser client look like the Rust iced desktop app: the same shadcn-style palette, typography, spacing, rounded controls, subtle borders, violet accents, icons, empty/error states and light/dark schemes. Compare real screenshots of matching screens (mailbox list, reader, composer, folders, Preferences, Google/profile pages) side by side against the installed desktop, in light and dark, and fix every visible difference; the mobile layout may adapt for touch but must keep the desktop look. Record the reviewed comparison captures under ignored `artifacts/` and keep the checks in the saved scenarios. Fixture screenshots are not proof until the reviewed comparison is recorded in the completion log.

- [ ] **R74 (client) — Restricted browser beta and Rust VPS service:** Rust Google-login verification, fail-closed owner allowlist, session/origin/CSRF protections and authenticated IMAP/POP3/SMTP transport are implemented with isolated production HTTPS evidence. Installation on the email VPS is authorized once the SSH target, exact verified owner identity and Google OAuth configuration are supplied; never infer the identity from Git metadata. Support administrator expansion of the allowlist later. **Remaining:** VPS installation/live verification, secure remembered client credentials, full account/calendar/backup parity, Outbox/cache bounds, move recovery and Sent history/labels. Keep mail/passwords out of persistent server storage and logs; retain client/provider ownership and conservative ambiguous-SMTP recovery. Browser login alone does not connect Gmail/Calendar/Drive.

- [ ] **R94 — Host shep.so through the infrastructure estate (requested 10 September 2026):** before hosting shep.so, move the VPS infrastructure (dungeonwalk-infra and the samruff.dev ansible) into the `sam-ruff/infrastructure` repository and add shep.so to the dungeonwalk box from there. Give agents working in the infrastructure repository the ability to manage Cloudflare with well-scoped tokens. Add staging on sophie: one LXC container per site (shep.local, dungeonwalk.local and so on) running the latest image from the LAN zot registry, updated via CI as images land. Fix the VPS website deploys to pull from zot instead of Docker Hub; the dungeonwalk box already pulls the solderforge images from zot, but the samruff box and dungeonwalk's own GHCR images do not. The website image goes to zot as `shep/website`; shep.so's quality/release CI stays disabled and the website has its own image workflow. Sam approved the first deployment once tests pass. Done: the site image and `Website` workflow are on main; the workflow builds and tests on the sophie pool and pushes only once `ZOT_USERNAME`/`ZOT_PASSWORD` exist. **Remaining:** the infrastructure migration, Cloudflare tokens, a `shep` zot project and its repository secrets, staging containers and zot-fed VPS deploys, a runner-template fix so Docker can resolve its `cache.local` proxy (the website workflow pins the address per job for now), then the shep.so site entry, DNS and first deployment. A lane brief for the migration is saved under ignored `artifacts/website/`.

- [ ] **R65 — Background mail freshness (client parity):** Desktop delivery (d4ecb21) is integrated. Browser visible-tab and Flutter foreground checks use 15-second intervals and independent queued manual refresh. Read-on-leave, explicit unread precedence, current pending-field projections and counted six-second Archive/Delete/Move/Restored feedback have client controls. **Remaining:** OS background scheduling, window/tab close and forced-termination durability, broader lifecycle/polling/error retention, Apple execution, large-cache performance and durable move recovery. Preserve newer intent, acknowledged identities, aliases, partial rollback/Undo, account removal and independent browsing while providers are blocked.

## Mail reading, search and bulk actions (client parity)

- [ ] **20 September desktop bulk delete responsiveness:** **Owner clarification:** fast review and immediate list feedback are required; actual server deletion may continue in the background. Provider batching is not a completion prerequisite. `a1049ab` freezes and summarises review in one database-worker transaction without waiting for earlier provider writes. Ten-message native review/confirmation, pre-admission Undo and rejection recovery pass with reviewed images; 38 native regressions pass. The 100,000-membership regression guards selected-row query plans; it is not a latency measurement. Remaining: measure the native 100 ms target at mailbox scale, implement Flutter/browser equivalents and verify the released/installed build.

Desktop delivery of these requests is recorded in the completion log; the entries track the remaining mobile/browser work and any desktop gaps named explicitly.

- [ ] **R38 — Faithful HTML:** Shared Rust/native/WASM preparation, confined browser frames and persistent Flutter WebViews preserve tables/CSS, CID WebP images, plain text, quotes, selection/Copy/Find, reviewed links and retry. Sender scripts and automatic external requests stay blocked; Flutter iOS minimum is 14.0 for shared WebP resources. **Remaining:** Apple execution, native/Apple and broader authored foreground/background contrast in dark mail, remote-image preferences/exceptions/loading, reading anchors during image reflow, bounded large-document layout and complete desktop parity. Use synthetic source examples and retain the existing rendering/control regressions.
- [ ] **R97 — Configurable previous thread in replies:** Sam, 11 September 2026: add a preference for whether replies include the previous email thread. The composer's per-reply **Include original message** checkbox stays and the preference sets its starting state for new replies (decide whether forwards follow it), saved with the other preferences and findable in Preferences search. It must not rewrite drafts already saved. Test the default, the per-reply override and restart, with native evidence.

- [ ] **R44 — Find in message:** Shared literal Unicode/whitespace search, visible highlights, case options, next/previous navigation, quoted-history scope and stale-result protection have native/host/Android/Appium/browser evidence. **Remaining:** full HTML/Find lifecycle, large-text bounds, mobile keymap configuration. Preserve offline/locked-credential behavior and reviewed highlights.

- [ ] **R18/R44 — Fuzzy matching:** Sam selected Abbreviations (`b00c784`) on 12 September for Preferences, Move and mail search. Nucleo ranks option/folder abbreviations with OSA typo fallback; mail keeps exact short-body, phrase and literal matches ahead of expanded matches, with stable paging and captured-selection ordering. Its twelve native scenarios and normal hooks pass on the demo branch; the 100,000-message multiword benchmark passes the unchanged 50 ms budget at p95 31.25 ms. Main integration and retirement of the other demo branches are tracked above. See the [search guide](docs/agents/search-demos.md) and completion log. **Remaining:** explicit handling beyond the first twelve query tokens, mixed alphanumeric identifier semantics (S3 still prefix-matches S30), and Flutter/browser relevance parity.
- [ ] **R41 — Forward:** Shared complete-source Forward and actual native/browser controls retain HTML/CID/attachments, blank recipients/new thread, atomic independent drafts, autosave and stable retries after lost acknowledgments. Preserve exact files, removal/restart, damaged-resource recovery, missing-credential refusal and late preparation that cannot replace a newer editor. Browser Forward is remappable/input-safe. **Remaining:** Apple execution, full composition/multiple-editor parity, large-mail bounds and live-provider verification.

- [ ] **R41 — Print:** Shared complete-source documents and actual browser/Android printer/PDF controls cover complete quotes, headers/filenames, inline images, aliases, independent navigation, cancellation/retry and paginated output. Browser Print is remappable/input-safe. **Remaining:** compile/run the UIKit adapter, other browser engines, native keymaps, permitted cached remote images, lifecycle/large-document/performance audit. Preserve desktop scaling, image-reflow reading position, software clipping and dock-badge behavior.

- [ ] **R42 — Multi-selection:** Browser worker SQLite capture/freeze and bounded gestures now drive Select/Select all/Clear/Done, checkboxes, Ctrl/Shift/Mod+A, cross-page ranges, immediate scope reset and recovery. **Flutter (mobile lane, 11 September):** the native SQLite capture now drives the Select button beside Search, desktop-style row checkboxes, long-press and `Select up to here` ranges from the anchor, captured Select all/Clear/Done and scope reset, alongside the durable group journal below; host, Flutter-web Playwright and Android integration/Appium evidence is in the completion log ([Mobile group actions](docs/agents/MOBILE_BULK.md)). **Remaining:** Apple execution, large-mailbox responsiveness measurements, and keeping the selection after a declined Flutter review (the capture is released when the review is prepared). Preserve 50-row observations, one in-flight/32 pending gestures, independent drafts during 100,000-message capture, explicit arrival recapture, range anchors, aliases, stale/lost observations and release. Selection must not read mail or consume text/sidebar/reader shortcuts. Imported desktop gap: explicitly selecting an arrival outside a prior capture must preserve the existing selection; background arrivals never join silently.

- [ ] **R42 — Bulk actions:** Browser frozen reviews, approved full-membership actions, owned durable execution, optimistic worker queries, 20-job/50-item History, Pause/Resume, Undo and explicit failure/uncertainty decisions are connected. Identity continuity follows acknowledged moves/validated aliases; receipts retain actual dispatch and inverse identities separately from frozen review metadata. The latest increment paints prepared Undo rows/counts before decision persistence or subsequent queries, preserves newer reader flags and reports rejection after History closes. **Delivered browser increment:** startup saved-group recovery in [`df4f29c`](https://github.com/sam-ruff/shep.so/commit/df4f29c2e12d21fc71353920696dd4958cde363a) uses bounded indexed observations, direct older-group review, retained targets on inspection failure, cache-only retry and ownership-aware tab-loss recovery. All 137 Chromium scenarios and 127 units pass; shipping evidence is in the completion log. **Delivered 10 September:** abandoned-review and staging cleanup (owner-token reviews with a per-tab liveness lock, a cancelled fence, bounded 50-row strict sweeps at the owner's wake and every 30 seconds, never a provider step), Undo evidence for changed scope/page, queued approval, partial failure, overlapping groups and unconfirmed results, and Y/N/Enter/Escape plus remappable approve/decline review keys for the review dialog and History; see the completion log. **Delivered 11 September (mobile Flutter bulk lane):** the mobile Rust crate keeps a durable group journal in the profile database (frozen 50-row staging, immediate projection, one owned step at a time through the shared per-message mutation path, receipts, Undo, Pause/Resume, explicit retry and unconfirmed-result acceptance, 20-group History with 50-item pages, conservative restart and the account-removal fence), with Flutter review, progress, completion and History controls; see the completion log. **Remaining:** cross-account transport, large-group performance (each IMAP step opens its own provider session), live-IMAP and Apple execution of Flutter groups, Flutter painting before the durable decision (the browser paints first), a separate repair queue for cache failures after a provider acknowledgement, and a decision on review keys for hardware keyboards on mobile (currently browser and desktop only). Preserve optimistic acknowledgement, newer per-field intent, exact membership, source/cache fences, cache-only repair, conservative uncertain restart and provider receipts; never automatically repeat an ambiguous write. Desktop's remaining group/individual coordination and History/recovery audit stays open. Latest evidence and shipping are in docs/COMPLETION.md. Found in the 11 September merge review of the Flutter group lane (integrated as `01569b5`): an unconfirmed step in a **Mark read** group says "Shep will not repeat this move", so the recovery copy must name the actual action; and on the 411 dp Android preview the Inbox header truncates "65 unread" because the PREVIEW badge and the new Select button share the bar (production builds have no badge, but check the narrowest supported width).

- [ ] **R45 — Drag mail to folders:** drag one mail or a selected group onto sidebar folders; highlight valid destinations, respect cross-account preference/provider support, and use the bulk-move confirmation. Desktop shipped in ca39080; browser/Flutter equivalents remain open.

## Mail, folders and desktop integration

- [ ] **R91 — Channel-owned backend state:** use bounded channels and state-owning workers for coordination, rather than shared lock-managed state. Account/calendar scheduling, the mail-cache connection/local leases, profile catalog and OS credentials have bounded owning workers, with ordering, cancellation, draining and failure tests. Database transfer has its own connection/controller and leaves the cache worker available. SQLite was updated after reproducing its Unix WAL open/close deadlock in the notification restart test. Channels must keep UI work nonblocking; do not substitute an unbounded queue. R64 validation exposed one shared-core Journal reopen `Owned` failure after drop; exact unchanged and full-history reruns pass. The lifecycle/Google lane checkpoint replaces the engine's Google RwLock, connection lifecycle and calendar setup mutexes with bounded FIFO lane coordinators and the shared token mutex with a thread-owned token vault whose admitted refreshes persist after caller cancellation; a Linux child-process regression proves the journal lock descriptor is close-on-exec and never inherited, and 15 shared-crate reruns show no `Owned`, so fork/exec inheritance is not confirmed as the R64 cause (retain r64 hook/rerun evidence). Remaining: independent-process Google/lifecycle coordination and live Google verification. The backup lane checkpoint `d36dca8` (`codex/backup-journal-owner`) replaces the backup journal's shared connection lock with a bounded FIFO owner, preserving keyed constructors, exact reservations and durable close draining; it is integrated on main as `42c69b4` with 935 lane hook executions, 114 backup tests, 84 Python tests and eight native backup scenarios passing; pushed in `c414227`.

- [ ] **R90 — Slow sync/close investigation:** reproduce unexpectedly long synchronization and shutdown, identify whether shutdown waits on unnecessary background work rather than required durable saves/receipts, and fix confirmed causes with deterministic slow-backend and native close/recovery tests. Distinguish live-server delays from local scheduling bugs; preserve actual in-flight write acknowledgments. Coordinate with R86 temporary tray while saving. The channel-coordinator fix interrupts a held read-only download for interactive writes and retains ownership until in-flight cache writes settle; deterministic and native close/read/flag regressions pass. Shutdown continuation, ordered errors, owned attachment/discard/forward failure recovery and interruptible pre-claim provider waits are integrated in `2733cc6`; 32 targeted tests and mandatory hooks pass. All 23 selected native scenarios pass on the integrated binary, and this source is pushed to main. Finish native/temporary tray handling and remaining personal-account diagnosis; the source checkpoint is not installed yet. **Sam, 9 September 2026: the first fix after lane consolidation is closing the app when saving blocks it.** Closing must never hang on saves: reproduce the blocked close on the current build, make the required durable saves finish in the background (temporary tray per R86) or surface a bounded, cancellable wait with visible progress, and prove it with deterministic slow-backend and native close scenarios before anything else in phase 2. **10 September blocked-close checkpoint (integrated as `c146e55`):** three causes were reproduced deterministically on `c414227` and fixed. First, tray-menu Quit and Preferences Quit Shep while a close was already pending re-entered the same `WindowClose` wait and did nothing, so a backup upload (deliberately untimed by design) or a credential-store call could keep a hidden, windowless Shep alive with no way out; a repeated explicit Quit now leaves at once when only journaled work remains (`backup:*` upload journal, `credential-cleanup`, both retried on the next launch) and otherwise reopens the window with the exact reason plus "Shep will quit as soon as this is saved" while keeping close intent, then exits on that acknowledgment. Second, Quit from an ordinary hidden tray gave no saving notification; it now sends the same temporary saving notice, whose text names Quit Shep. Third, after `iced::exit()` the daemon drops its Tokio runtime and that drop joins every running blocking task, so a stalled `spawn_blocking` transfer would leave the process running silently after the last window closed; `finish_exit` now arms a five-second exit deadline once every required acknowledgment is in. Local saves (drafts, attachments, preferences, journals, receipts) and unjournaled provider writes (send, event, account, outgoing recovery, Google) still block exit and remain bounded by their provider timeouts. Evidence: seven new `ui::tray`/`ui::closing`/`lifecycle` tests (three shown failing on unmodified `c414227` in `artifacts/logs/close-repro-head.log`), the new `backup_run="held"` fixture that stalls the second upload inside a blocking task, and native scenarios `test_tray_native_quit_leaves_held_backup_upload_journaled_and_process_exits`, `test_tray_native_close_during_slow_backup_upload_notifies_then_exits_when_saved`, `test_tray_native_quit_during_held_readonly_sync_exits_and_keeps_cache` and `test_tray_native_repeated_quit_with_pending_send_shows_reason_and_keeps_reply` (process exit checked through the harness return code, journal row and cache counts checked read-only after exit and restart, light and compact dark WebPs of the reason notice reviewed). Still open: the actual personal-account diagnosis (which save blocked Sam's close is inferred from code paths and fixtures, not observed live), Windows/macOS execution of the tray Quit path, and whether provider-slot starvation of a durable save behind eight read-only jobs should also be interruptible.

- [ ] **R86 — Native close-to-tray and temporary saving tray:** closing the main window should hide Shep in the native system tray/menu bar, with Restore/Open and explicit Quit actions. Keep background mail sync and notifications working, provide a preference, and handle desktops without tray support gracefully. Cover Linux/Windows/macOS behavior and preserve pending drafts/operations on actual Quit. Follow-up: even with close-to-tray disabled, closing while saves are pending should temporarily hide to the native tray, notify that Shep will quit once saving finishes, and then exit automatically. Preserve failure recovery and allow reopening; do not silently lose drafts or pending writes. The original tray request was backlog-only during the handover; this follow-up also asks to investigate excessive sync/close delays. Active parallel lane `codex/native-tray` is implementing the iced daemon/window lifecycle, Linux StatusNotifier adapter, Windows/macOS native menu adapters and deterministic/native host tests; implementation includes native Open/Quit, temporary saving notices, window reopening and an isolated native tray harness. Nine targeted Rust, 58 Python and all 26 selected native scenarios pass with reviewed WebPs, full Windows GNU checking and exact macOS adapter checking. Mandatory hooks and root integration are underway; actual Windows/macOS execution, full macOS app checking and production desktop review remain pending. **10 September:** the temporary saving tray now has a way out: a second explicit Quit (tray menu, Preferences Quit Shep, or closing the visible fallback window again when notifications are unavailable) leaves immediately when the remaining work is journaled, reopens the window with the reason otherwise, and the notification text says so; Quit from an ordinary hidden tray announces saving the same way; process exit is bounded by a five-second deadline after the final acknowledgment. See R90 for the reproduction and evidence. Windows/macOS execution of these paths is still unverified.
  Tray source `6728931` is pushed with 745 hook executions and 32 integrated native scenarios passing. Root reproduced hidden failed sends after ordinary close-to-tray. The correction is pushed in b8eafde:12 tray/controller tests,748 hook executions and16 selected native scenarios pass, including Quit from an already-hidden window, older-result ownership and queue backpressure. Actual platform execution and remaining personal-server diagnosis stay open.
  **Sam, 11 September 2026:** close-to-tray should default to on. Today the preference defaults to off (`close_to_tray: false` in `src/model.rs`). Turn it on for new installs; check whether saved preferences can tell "never changed" from an explicit off, and if they can, switch the untouched ones too. Keep the no-tray fallback: where the desktop has no tray host, closing still quits normally with pending saves protected.

- [ ] **R30 — Folder trees and context menus:** nested folders form collapsible groups, collapsed by default, honoring each IMAP hierarchy delimiter and nonselectable parents; delete folders and move them inside another folder, reviewing destructive scope and keeping account/server/cache state consistent with failure/retry tests. Desktop trees shipped in 6d520e9 and reviewed Move/Delete, nested groups and recovery controls shipped in 3567de2. Finish combined-folder optimistic scopes, aggregate common-folder account choice, history paging/account removal and cache convergence after accepted uncertainty. Keep original mail when an outcome is unconfirmed; acceptance is not server success. Pending-rename Ctrl-selection and filtered header counts are verified in `66e6cb8`; integrated and pushed as `97c9a9a` with 19 native scenarios and 818 hook executions passing. Combined-folder deletion checkpoints `6d7c392` and `9b300af` are being integrated; exact affected query counts are a bounded scalar with indexed scratch membership. All 763 lane hook executions, 47 targeted folder tests, fifteen native scenarios and 56 Python tests pass. Integrated and pushed as `1c0fc45`, with 915 hook executions and all 273 native scenarios covered across two invocations. The verified `codex/aggregate-folder-choice-v2` checkpoint `1abc4b9` adds explicit account choice for aggregate common-folder actions and configured Sent identities, with five controller cases and reviewed mouse/compact-dark WebPs; it is integrated on main as `d99b111` with 944 hook executions, 86 folder tests, 84 Python tests and 19 native folder-control and sidebar scenarios passing; pushed in `ee1305c`. Broader scope/page reconciliation remains open. Flutter/browser folder trees and context menus remain open in the parity matrix: after the main merge the shared sync emits the desktop's `Mailbox` hierarchy, but the mobile cache and the browser gateway still keep only flat selectable names and drop inbox lifecycle events.
- [ ] **R35 — Inline composer:** new messages/replies open in the preview pane; autosave while typing and support switching among multiple drafts and received mail without losing content, recipients or attachments. Desktop inline replies and independent draft sessions shipped in e16590c. Browser regression found during read-on-leave verification: multiple saved drafts inherit the three-column mail/5 px divider grid, making one draft overlap another. Saved drafts now have an independently scrollable, clickable list with correct counts; compact Playwright controls reopen every item and the real HTTPS account-removal/draft regression passes. This does not complete inline/multiple-editor parity on the clients. **Sam, 11 September 2026:** in the desktop inline reply pane the Conversation list under the composer is oldest first (his example shows 07 Sep 10:00 at the top and the newest 11 Sep message at the bottom), so reading the latest message means scrolling. Show it newest first: the last message at the top, then the second to last, and so on. Check whether the reader's own conversation view should follow the same order, and keep paging, focus and draft-parking behaviour.
  **Desktop ordering shipped:** `8d2a63d` puts the newest message first in both
  reader cards and inline-reply history, with indexed Newer/Older paging and
  preserved focus, tied-date order and parked drafts. Relevant store/native
  checks and normal hooks pass; client conversation parity remains open.
- [ ] **R73 — Moved mail missing from destination:** recovery/projection checkpoint a81d767 is shipped. Finish actual adapter wire/journal integration, broader Undo/bulk/folder-history lifecycle and repeated-move aliases, plus independent-process coordination with R01.
- [ ] **R47 — A. Keep → Inbox:** confirm the personal-account failure's root cause and actual destination refresh. Commit 590ab10 ships Inbox labeling, send-queue feedback and a spaced-folder protocol test preserving acknowledgment after logout failure; live diagnosis remains open.
- [ ] **R50/R60 — Optimistic interaction everywhere:** archive/move, flags, read/unread and other reversible actions show their expected result immediately, before network/SQLite completion; archiving removes the mail from the current list immediately and restores it with an actionable error on failure. This is the app-wide optimistic interaction principle, recorded in AGENTS.md. Immediate flags/read/move/Undo feedback is shipped (742b21e, d3a530a, 9c907d2). Finish membership/count reconciliation when switching filtered or combined folders, ambiguous cross-account outcomes, other reversible controls and durable restart recovery. Preserve newer intent and visible rollback errors. The filtered unread-header correction is pushed as `97c9a9a`; broader combined-folder deletion and uncertainty/restart work remains. Client checkpoint: the Flutter inbox and open reader share persistent mutation errors with Retry/Dismiss; host and Android controls verify missing/locked-credential rollback. The remaining app-wide audit stays open.
- [ ] **R82 — Native new-mail notifications:** the 12 September GNOME sender-lifetime fix is committed as `ae85450` and installed, with a bounded worker-owned connection for arrivals and temporary-saving notices, protocol/mock tests and reviewed actual GNOME evidence. Default-on popup/sound/details and independent preferences shipped in ded5aca; preserve quiet initial imports and do not replay uncertain delivery. Live-provider verification, actual Windows/macOS and Wayland delivery, macOS bundle integration, sound review and Flutter/browser equivalents remain open.
- [ ] **R70 (desktop) — Dock/taskbar new-mail badges:** show an unread Inbox count on the Shep launcher/dock/taskbar, including the GNOME-style badge in the user's screenshot where the desktop supports it. Reconcile new arrivals, read/unread, optimistic moves/deletes/Undo, account scope and restart without stale counts; allow disabling badges in Preferences. Use native platform integration with isolated automated tests and document actual desktop/platform support. Linux Unity/Dash-to-Dock adapter and preference shipped in 90776fa with a coalescing background worker and real private-bus tests. Windows taskbar overlays and native macOS Dock labels are integrated/pushed in 1595fb3, using bounded count delivery and shell/window lifecycle recovery. The integrated source passes 751 hook executions, 59 Python tests, 12 native Linux scenarios, full Windows GNU checking and exact macOS adapter checking. Actual Windows/macOS execution, desktop rendering and ambiguous-provider/restart count reconciliation remain open alongside R50/R60.

## Accounts, portability and backups

- [ ] **R76 (client) — Scheduled out-of-office replies:** add **Preferences → Automatic replies** for one account, selected account groups or all accounts. Use a list of named reply entries: type the message once, set its schedule, then choose accounts with a searchable multi-select, saved account groups and **Select all**. Add another entry for a different message/group without retyping the first (for example, Work and Personal). Include start/end date and time, a visible timezone, clear active/upcoming/ended status, and easy edit/disable controls. Validate end-after-start and resolve overlapping assignments explicitly. Implement and show provider support/setup and per-account results; unsupported or failed accounts must never appear enabled. Prefer server-side scheduling so replies work while Shep is closed. Cover account/group assignment, scheduling boundaries, timezone handling, overlapping edits, partial failure/retry and client parity in provider/UI/E2E tests.
- [ ] **R32 — Multiple backup destinations and options:** polished destination list/setup for Local, Google Drive, S3, FTP/FTPS and SFTP with independently configurable connections and easy extension points; compression and chosen passcode encryption at setup, password prompt on restore, multiple destinations enabled together, independent upload results and configurable per-destination rolling copies (e.g. ten unreadable encrypted archives). Local/Drive destinations and S3 setup/recovery are pushed (`2b87db4`, `4e75005`), with independent schedules, retention, passphrases and native verification. SFTP is pushed as `ff03b02`: verified host fingerprints, staged upload/resume/restore and compact setup/retry/restart. Root reproduced and fixed the unbounded SSH channel-confirmation wait; all 16 SFTP protocol tests and 18 integrated native scenarios pass. FTP/explicit and implicit FTPS checkpoint `17de158` passes 802 lane hook executions, 28 FTP/SFTP tests and six native scenarios; integrated and pushed as `13f9c36` with 834 hook executions and 13 native scenarios passing. Finish richer per-destination failure history and remaining authentication/platform/live verification. Keep bounded journal recovery and never delete unrelated files; see [the backup contract](docs/agents/MULTIPLE_BACKUPS.md). Combined-manual checkpoint `c515b80` adds persisted Include choices, bounded Back up all admission, separate progress/error/Retry/Setup rows, exact-ciphertext retry/restart and close acknowledgments. All 811 lane hook executions and 11 native scenarios pass; integrated and pushed as `cf76976`, with 843 main hook executions and 13 native scenarios passing. Optional-format checkpoint `7ef6c56` adds per-destination compression/encryption, backward-compatible restore and immutable pending-upload format/passphrase identity. All 824 lane hook executions, 98 backup-filter tests, twelve native scenarios, full Windows GNU checking and strict docs pass; integrated and pushed as `1c0fc45`, with 915 hook executions and complete native scenario coverage. History checkpoint `895fe2d` adds bounded persistent per-destination success/warning/failure history, restart-safe recovery visibility and target-labelled final feedback for mixed formats; confirmed copies with setup/cleanup warnings remain distinct from failed uploads. On the lane it passes 109 backup checks, 28 transfer checks, 22 native scenarios with reviewed light/compact-dark WebPs, full Windows GNU checking, Python and strict docs; it is integrated on main as `c35e2b5` with hooks, 84 Python tests, strict docs and 31 selected native scenarios passing; pushed in `c414227`. Journal channel ownership follows separately as R91; remaining live/platform verification stays unfinished. Flutter/browser backup parity remains under R67.

## Storage, appearance and installation

- [ ] **R22 — Encrypted local cache:** encrypt SQLite/mail/attachment data at rest and safely migrate existing personal data; keychain credentials and encrypted backups alone do not fulfill this request. SQLCipher/key access, keyed Store/journals/profile catalog and scoped temporary-storage policy are shipped in `1c0fc45` with 915 hook executions and complete native scenario coverage. Production startup is still plaintext. Guarded legacy publication and crash recovery are now implemented in `cache_cipher::publication`: TRUNCATE checkpoint and DELETE-mode close of the legacy WAL, a journal beside the cache, same-folder renames with directory fsync, keyed authenticated verification before the plaintext recovery file is deleted, deterministic resume/rollback from every interrupted step (wrong key restores the plaintext while it exists and otherwise keeps everything), and guarded orphan cleanup of stale candidates/scratch; ten publication tests cover each step, retry, held connections, foreign guards, lost candidates and ambiguous layouts (28 cipher tests). The bootstrap checkpoint adds `cache_cipher::bootstrap`: production startup now goes through one owning `shep-cache-root` worker (bounded channel of one request) that holds the exclusive guard from key lookup/creation through recover/stage/publish of every database in the root, re-takes it shared and hands `Root { key, guard }` to the catalog, active store and backup journal workers, which release it only after their admitted writes drain; the production policy `Existing` keys only roots that already own a `.cache-root` marker, so every current install stays plaintext while `Migrate` (key creation and conversion) is test-only; import staging in a keyed workspace converts logically into a keyed copy with keyed fences/installation; older guard-less Shep processes are excluded by the WAL-exit refusal of the checkpoint step, proven with a subprocess fixture. Concurrent starts wait briefly for another Shep's opening phase, and the keyed import copy keeps the source application ID. Lane commit `400b836`, merged with `main` at `ca28e69` on the lane: 40 `cache_cipher` tests (ten bootstrap), 15 profile and 32 import tests, 1117 lane and 1132 merge hook executions, 96 Python tests, Windows GNU checking and eight native database import/export scenarios pass; not pushed. Still to finish before activation: a user-facing or setting-driven way to choose `Migrate`, reader-guard retention in the profile transport/history workers, bounded selection-summary/catalog/recovered-view sorting and the `sqlcipher_export` index builds in migration staging and keyed import, native key recovery, a native migrated-root scenario and actual Windows/macOS startup checks. Keyed raw export checkpoint `f0ff98ac` preserves user-selected portable SQLite output, exact rowids and cancellation; 815 lane hook executions, seven export tests, 18 cipher tests, eight native scenarios and full Windows checking pass. Conversation ranking checkpoint `8d45a9e` replaces whole-thread sorting with indexed encrypted scratch and corrects scratch to DELETE/OFF while main remains WAL/FULL; its 819 hooks and eight native paths pass. Both are integrated on main as `c35e2b5` with hooks, 84 Python tests, strict docs and 31 selected native scenarios passing; pushed in `c414227`. No personal database has been modified; see [the encryption inventory](docs/agents/CACHE_ENCRYPTION.md). The client SQLite/IndexedDB caches are still unencrypted.
- [ ] **R23 — Large mail:** remove the incoming 25 MiB, snapshot 256 MiB and preview 32,000-character ceilings through bounded streaming/paging and background large downloads that do not hold up small mail. Ordinary native caches now receive incoming large mail through staged partial IMAP FETCH or streamed POP3, after smaller mail, and atomically publish exact SQLite blobs. The skipped-size bar is removed. Reader pages advance by the requested budget even when HTML markup yields little visible text; refresh and late results preserve expanded pages. Still open: encrypted staging, fully windowed reader/attachment decoding, complete body search and cumulative decoding limits. Explicit reader opening still loads raw/attachment bytes, deferred download metadata is retained, and async-imap's literal allocation precedes partial-response validation for hostile servers. SQLite's physical blob limit remains. Flutter/browser receiving parity needs its own staging integration. Snapshot restore still rejects more than 256 MiB because archive validation/rebuilding is in memory; outgoing 25 MiB and 18 MiB attachment limits are not yet audited. These changes do not establish full R23 streaming or performance completion.
- [ ] **R25 — Palette editor (client parity):** the desktop palette editor with persistent light/dark schemes shipped in `4e75005`. Flutter and browser palette configuration and the shared palette remain open alongside R80.
- [ ] **R64 — Launcher and tray icons:** preserve the approved Shepherd with a transparent background, an opaque light face on GNOME's dark shell surfaces and matching launcher/tray colours. The tray must fill its slot with crisp contours at 1x/2x. Main `b5546c7` corrects the faint symbolic launcher and padded tray, with actual GNOME before/after review of the dash, app grid, Alt+Tab and top bar at both scales. GNOME's panel stays dark in the light application style. Four targeted Rust, 103 Python, two lane native and five integrated native scenarios pass, along with 1,182 normal-hook executions and documentation CI; the completion log records the independent owned GNOME captures. **Remaining:** actual Windows/macOS shell execution and personal installation; asset/template checks alone do not establish those platforms. Keep original references and never restore the rejected faint face or padded tray.
- [ ] **R61 — Direct download installers:** GitHub raw scripts for Linux/macOS/Windows, home-directory installation by default, optional all-user/elevated installation, native application-menu integration and isolated download/extraction/update/cancellation tests. These are now the first install options in README and install docs. Linux `3676dad`, macOS `0f2af1f` and Windows `ce4a2a6` are pushed with normal hooks and green docs CI. All 81 Python tests pass, including actual PowerShell execution with explicit native boundary fixtures on Linux. Actual OS execution and published-asset installation remain distinct from fixture verification; binary releases are not published yet. **Sam, 11 September 2026:** fix the releases so the README and install-doc one-liner `curl -fsSL https://raw.githubusercontent.com/sam-ruff/shep.so/main/scripts/install-release-linux.sh | bash` actually works. That needs published GitHub releases carrying the assets and checksums the installers expect. The release workflow is still disabled; the lab's runner pools now serve Linux and Windows, so confirm with Sam before enabling it. Publish a release and verify the one-liner end to end on a clean Linux user account, then the macOS and Windows equivalents.
- [ ] **R15/R17/R21 — Final usability review:** finish redundant-copy, spacing, icon sizing/alignment, compact/large-font and clipped Shortcuts-label checks. Singular message-count labels are corrected in the HTML-reader work. Preserve the approved dog logo, mouse usability, saved draggable panes, WebP assets and clean shadcn-style controls. Filtered-settings renderer correction 0d2feb1 is shipped with direct renderer and saved native pixel regressions; all 109 native functional scenarios passed and the release was installed. The requested History header icon and tooltip shipped on 11 September as `f6b5d85`, with native mouse/Escape/compact-theme, paging and restart/retry evidence in completion; browser/Flutter header comparison stays open under R80. Found during the 11 September password-vault review: unchecked checkboxes are hard to see in the light theme across the app, and the Account passwords card repeats its toggle label in the line "Passwords are synced through your Google account."
- [ ] **R98 — Help tooltips for easily misunderstood settings:** Sam, 11 September 2026: add a small **?** icon beside settings whose meaning is not obvious, showing a short help tooltip on hover or keyboard focus. Only for easily misunderstood options, not every setting; candidates include Google-only password sync, backup encryption and compression, cross-account moves, the background check interval and close-to-tray, with the final list chosen during review. Respect the existing Tooltips preference, keep the text readable in light, dark and compact layouts, and make it reachable without a mouse.
- [ ] **R99 — Settings search covers every setting:** Sam, 11 September 2026: search every setting's label, tab, section and description plus common synonyms, including backup, account, Google and profile cards. Sam selected Abbreviations on 12 September. Its expanded catalogue ranks visible labels first, navigates across tabs, handles no results and guards persisted configuration-field coverage. Complete dynamic-caption coverage, individual-control reveal and Flutter/browser parity remain open; matching a section does not establish exhaustive R99 completion.

## Verification and shipping

- [ ] **R93 — Parallel delivery:** use multiple agents in isolated Git worktrees to progress independent TODO features concurrently. The primary agent integrates tested commits into `main`, resolves conflicts and runs combined regressions before pushing. Run isolated correctness-only native flows alongside capped unrelated builds; coordinate quiet pixel/latency measurements and idle-host performance windows. Preserve all existing requests and record each lane's evidence and limitations. The request to finish every TODO supersedes earlier backlog-only staging, without waiving tests or shipping requirements.

- [ ] **R63 — Final E2E functionality-path audit:** map every visible feature to realistic native/browser happy/error/recovery scenarios with saved automated equivalents and reviewed screenshots as remaining features land. Remapping/clear controls and rapid native keyboard ordering are shipped; preserve deliberate rapid-input tests without extra waits. Investigate intermittent GTK file-picker startup/clipboard readiness: one integrated export setup failed with an unpainted picker, followed by three unchanged passing reruns; evidence is retained under `artifacts/e2e/ee7f4ccad7e2`. The integrated 248-flow run repeated the unpainted GTK picker failure (`1a953a52acb2`); all 19 affected native paths now pass with an isolated Cairo-rendered picker, unchanged production rendering/clipboard proof/timeouts, and reviewed WebPs. The cross-page deletion test now awaits actual last-row reveal and passes; both corrections are pushed in `15a4a3c`. The held-upload first-profile fixture is shipped in `91c59a4`, with eight integrated native scenarios and 84 Python tests passing; pending-navigation assertions and timing budgets are retained.
  Client continuation: the badge re-enabling regression checks the current unread count after read-on-leave navigation, retaining native bus assertions and failed-before evidence in the completion log. Retain held-pointer/footer controls, list/reader/text shortcut isolation, divider settling, read-on-leave and native editor-focus regressions. Browser shortcut capture preserves held presses/modifiers through background redraws and saves/cancels current settings. History progress no longer waits for Undo preparation; failed previews retain visible Refresh recovery. Final regression passes 133/133 Chromium scenarios and 123 unit tests; verified shipping is recorded in the completion log ([`1ea12a6`](https://github.com/sam-ruff/shep.so/commit/1ea12a677829dcd71c4246b87cae46327f9c749f)). **Remaining:** rapid native input during concurrent builds, wider list/press and full keymap/focus lifecycle, complete native/Apple equivalents and overall feature coverage. Failed-before traces remain in artifacts/browser-capture-failures/. Native index verification also reproduced compact bulk checkbox and badge-preference input failures during compilation; saved screenshots/state and the unchanged no-build rerun are recorded in the completion log. Keep this input-under-load audit open. Never hide failures with forced clicks, direct controller calls or relaxed deadlines.
- [ ] **R01/R06 — Provider/platform completeness:** finish POP3 malformed/duplicate UIDL and lifecycle gaps, rejected-command handling in remaining async-imap helpers, Google/CalDAV/SMTP integration evidence, independent-process write coordination and actual Windows/macOS execution/distribution. Distinguish fixture from live evidence.
- [ ] **R03/R09 — Final performance gates:** measure only at the end on an idle host; do not weaken budgets. Keep quality/release CI disabled until requested. Documentation CI has a separately authorized exception. Client continuation: exact FTS materialization and the covering unread-account index pass the unchanged backend budgets (search p95 35.54 ms on 100,000 rows). All 118 functional flows passed before the final index, and all 14 relevant index controls pass with compilation stopped. Native navigation still measured 154.81–162.33 ms against 150 ms across four runs on the client branch before the merge; investigate the retained timing evidence and re-measure the merged tree. The combined performance gate remains failing; do not relax it or claim full responsiveness completion.
- [ ] **R08/R10 — Ship remaining verified work:** every session pushes verified checkpoints to `sam-ruff/shep.so` main promptly (the client and website worktrees rebase onto `origin/main` and `git push origin HEAD:main`), without waiting for full product parity. Run applicable hooks/Rust/Python/native checks and release/installer verification before installing production updates; retain failed evidence and record limits. Do not replace the installed desktop from client-branch builds. Keep logs under ignored artifacts and preserve personal data. `main` is the single integration branch for all sessions; the handover push is a source checkpoint, not a new installed release. The user explicitly resumed client development on 2026-09-08; continue full parity from the handover, with OAuth/shared profiles first, and keep pushing verified checkpoints.

- [ ] **R15/R63 — Native dropdown keyboard dismissal:** account-review native testing reproduced Escape leaving an iced pick-list open; the following click can select a menu row covering another button. Add consistent Escape/keyboard handling and realistic native regressions without triggering background shortcuts. Branch `fix/dropdown-escape-dismissal` routes every native pick list through `ui::dropdown::pick_list` (Clippy disallows the plain iced constructor): an open menu takes every key press, Escape or Tab closes only the menu, and no dialog, composer, Find or mail shortcut sees those keys. Five runtime-routing unit tests (three failed first on the plain pick list); three saved `test_dropdown_*` native scenarios cover the event dialog (light and compact dark), the mail filter with Find open and the composer From list. Remaining: integration on main; arrow/Enter keyboard navigation inside an open menu (iced 0.14 keeps its highlighted row private); Flutter dropdown keyboard review (no Flutter changes this wave). The browser uses native `<select>` elements and needs no change.

- [ ] **R15 — Distinguish duplicate-address accounts:** shared endpoint reviews retain the old account and add the new connection, but sidebar email-only labels look identical. Show the existing account names clearly for these duplicates, preserve narrow sidebar layout and verify native mouse/keyboard account choices. Checkpoint `d5c7683` shows each duplicate's saved name with the address beneath in inbox children and account headings, with a unit test and a native scenario covering mouse and Up/Down/Return choices, 160 px minimum width and 900x640; integrated on main as `c0ebf3f` with 939 hook executions, 84 Python tests and seven native scenarios passing; pushed in `c414227`. Two duplicates with identical or empty names remain indistinguishable, folder rows under duplicates and the light theme are not captured.

- [ ] **R63 — Long native-run termination:** integrated correctness run ended with signal-style exit143 after20m17s and269 passing scenarios, during the ordinary tray-failure scenario. No assertion failure was emitted. All four remaining tray scenarios pass unchanged in17.300s on the same binary; inspect runner/harness termination and preserve the interrupted-run receipt rather than calling it one clean full run. Owned-PID cleanup review found no broad process termination.
