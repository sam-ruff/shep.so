# Shep TODO

Active requests from this chat. Add new requests here immediately, including corrections. Keep an item until implementation and relevant automated/native verification are complete; remove it only while recording its evidence in [the completion log](docs/COMPLETION.md). [The request audit](docs/REQUEST_AUDIT.md) maps the complete conversation, including work already delivered. The full product goal is still active.

## Mobile, web and website (same monorepo)



- [ ] **R67 — Flutter parity:** build the Android/iOS client in `flutter/`, preserving Shep's theme, approved logo, configurability and complete mail/calendar/provider/backup behavior. Correction: keep one monorepo, with Rust desktop at the root, `flutter/`, separate desktop-style `web/`, and promo `website/`; do not create `shep.flutter`. Maintain feature-by-feature evidence; previews do not establish provider parity. Native Rust bindings, device cache/accounts/credentials, durable drafts, shared Reply/Reply all and outgoing attachments have host/Android/browser-control evidence. Both clients preserve exact MIME before SMTP, reviewed Outbox decisions, local Sent, provider Sent reservations/recovery, atomic identity adoption, aliases, logical Sent grouping and physical Undo destinations. Local edits and terminal acknowledgments survive refresh/storage failures. Cached incoming files now use shared Rust/native/WASM decoding; Android real save/cancel and offline browser downloads verify exact bytes. Flutter retains an open reader when refresh excludes its list row. See the completion log for tests, visual review and checkpoint shipping evidence. **Remaining:** native/browser Sent copy labels and history cleanup, browser Outbox paging, bounded body parsing/prefetch/cache and reader lifecycle audit, incoming-file Apple execution and large/deep MIME handling, full composition/multiple editors, account lifecycle, calendar/Google/backups, current desktop defaults and full simulator/protocol parity. Reader-action continuation: browser and Flutter actions now have stable footers through asynchronous preparation. Flutter preserves counted Undo above responsive safe-area actions, stable Forward/Print labels, held-touch activation and scaled-text reachability. Shared host/Android controls, actual printer/PDF and formatted Appium regressions pass; final browser, artifact and prompt shipping evidence are recorded in the completion log. Compact 150% text testing also fixed overflowing Inbox filter chips while keeping Sort reachable. The Flutter browser check exposed desktop visual density shrinking footer targets below 44 px; standard footer density now preserves the real 44-pixel semantic/button bounds in saved controls. Apple execution and native keymaps remain required before claiming complete reader-action parity. Reviewed local account removal now has native/browser transaction, host and actual control evidence: current counts, stale-review rejection, busy-operation protection, removed-identity guards, immediate draft/reader cleanup and retryable device credential deletion. Server mail remains unchanged. Account connection editing, broader lifecycle notifications, Apple execution and large-cache performance remain open. Native credential handover now stages independent device keys and atomically activates matching settings; failed/lost acknowledgments, legacy TLS migration, stale request rejection and retryable cleanup have Rust/host/Android control evidence. Connection editing still requires reviewed mailbox-identity migration and its browser equivalent. SQLite remains unencrypted. Fixture success does not establish live-provider or complete feature parity.
- [ ] **R68 — Mobile mail interactions:** K-9-inspired sender/subject/snippet rows, account/unread/attachment indicators and swipe action icons; independently configurable left/right swipes, immediate feedback, undo/failure recovery, accessible clickable equivalents and persistent preview settings. Continuation: shared acknowledged IMAP receipts/exact-content lookup, stable client identities, durable unresolved intents and queued/post-refresh Undo now have protocol/cache/browser/Android evidence. Finish explicit review/retry for ambiguous or unrecoverable moves, cross-account behavior and the complete accessibility/provider-success audit; do not treat this as full move parity.
- [ ] **R69 — Flutter quality/release:** adopt Walkie Textie Flutter's Playwright plus native integration-testing approach, extend extensive happy/error/recovery scenarios, run Android emulator and Apple simulator checks where supported, review screenshots, and prepare disabled CI and coordinated monorepo releases. Earlier integration verified 14 Android scenarios across the full runs and targeted reruns; subsequent Forward and Print scenarios add complete-source and actual PDF controls. Existing coverage includes five formatted-reader Appium stages, six standard Appium stages, and both Flutter browser paths. Picker freshness/intent, asynchronous native highlight observation and the selectable Outbox body assertion are corrected; failed evidence is retained in the completion log. Apple execution, broader parity/lifecycle/performance scenarios and signed coordinated distribution remain open. Keep quality/release workflows disabled. Current verification also refreshes the frozen formatted-preview fixture from the current shared Rust renderer and checks it before both Android Appium and browser builds, preventing stale authored-color defaults in test previews.
- [ ] **R70 — Promo website:** use a delegated agent in a Git worktree to add `website/` with real synthetic-data screenshots, concise README-style copy, prominent user-agent-aware installation and all-platform alternatives, Android/App Store/native links with honest availability, and browser-app access.
- [ ] **R71 — Browser hosting:** build a separate browser client in `web/` matching Rust iced and deploy at shep.so, also supporting network access. Correction R74: a Rust backend on the email VPS is now authorized for beta verification and IMAP/POP3/SMTP transport; the static-only/local-bridge proposal is superseded. Retain client/provider ownership and no persistent server-side mail/password storage unless explicitly changed. Keep current development in worktrees; obtain VPS/OAuth configuration before deployment. Do not claim browser TCP sockets or preview behavior establish provider parity.
- [ ] **R72 — Linux app stores:** investigate and prepare Flathub and other appropriate Linux distribution/store submissions, packaging, metadata, screenshots and verification; track publication separately.

- [ ] **R73 — Continuous parity:** add an agent/development reminder and review checklist: every desktop feature/change must update mobile and browser equivalents or explicitly retain an active parity gap with tests. Hosted browser behavior must match Rust iced; the Flutter mobile layout is separate. Work only in Git worktrees until integration is requested.
- [ ] **R74 — Restricted browser beta and Rust VPS service:** require real server-verified login before the hosted client or mail APIs are accessible. For now allow only the owner's explicitly configured verified Google identity; support opening to additional users later through an administrator allowlist. Build/install a Rust backend on the existing email VPS for verification and ordinary mail protocols. Fail closed with no configured users, test unauthorized/denied/expired sessions and CSRF/origin protections, and prepare reviewable deployment files. VPS SSH target and exact permitted Google account/OAuth configuration are pending user input; do not infer them from a Git author address. Keep secrets out of logs/repo and preserve the no-persistent-server-mail/credentials requirement. Worktree checkpoint: shared Rust mail/MIME contracts, bounded authenticated transport and a basic browser provider/cache adapter are implemented and under regression testing. Remaining: full account/calendar/backup parity, incoming-file lifecycle and large/deep MIME handling, worker-based bounded cache/Outbox reads, remembered credentials, move recovery, provider Sent copy labels/history and live/VPS verification. Browser identity handover/grouping now has atomic cache and real Rust HTTPS control evidence. Browser Outbox review and exact local Sent now have automated fixture evidence. Outgoing attachment persistence and cached Reply all now have Rust/browser/Android fixture evidence, distinct from live delivery. Keep ambiguous SMTP delivery protected against automatic resend.

## Current UI and interaction work

- [ ] **R65 — Background mail freshness:** Committed desktop `10d8569` (33 commits since the shared ancestor) is integrated and shipped on the review branch in `72ca625`, preserving shared mail/model/provider contracts and both request histories. All 116 native functional flows, 372 Rust/shared tests and the client checks in the completion log pass. The separate main worktree’s uncommitted selection edits remain untouched. Desktop shipped in upstream `d4ecb21` with 15-second checks and independent queued manual Refresh (evidence `2965ae2`). Preserve the integrated desktop defaults when completing client background scheduling. Browser visible-tab and Flutter foreground checks are implemented; OS background scheduling/lifecycle verification remains open. Upstream also shipped relevance/body-load actions in d3a530a / 3375e8b and draft navigation/discard in 5aab267 / eb1ccc7; preserve/port these. Main’s R67/R68 read-on-leave/action-toast requests (shipped independently in 9c907d2 / f659926) use colliding identifiers: track them as desktop-main:R67/R68 during eventual integration; do not overwrite this thread’s mobile requests. Main subsequently shipped responsive move Undo and acknowledged move receipts in 551f86c / 1c1372e; the shared receipt/exact-recovery contracts are now ported into both clients with tests. The current read-on-leave increment ports native Flutter and browser controls: only deliberate reading arms it; navigation, composer entry and losing focus finish it; explicit Mark unread wins; background refresh/prefetch never acknowledge unseen mail. Preserve ordered provider mutations, immediate feedback, rollback/retry, aliases and the new reader while older saves finish. Native cache-query projections replace pending-mutation page deferral; filtered pages, global counts and confirmed rollback values share one read snapshot while provider work remains pending. Host, native SQLite/protocol and actual Android/browser controls verify the increment; see the completion log for execution and shipping evidence. Window/tab-close durability, forced termination and OS lifecycle/polling/error-retention audit, Apple execution, large-cache performance and remaining durable move-recovery UX stay open. The current increment adds counted Archive/Delete/Move/Restored notifications with a six-second lifetime, destination/account grouping, Dismiss and grouped Undo. Keep token-scoped failures from clearing a newer notification or unrelated Undo, preserve pending/acknowledged ordering and physical destinations, show controls in both the inbox and native reader, and preserve expiry, repeated moves, partial failure, refresh/aliases and account-removal regressions. Host and Android controls now cover grouped Undo and restoration ordering; browser unit/Playwright and real Rust HTTPS checks pass. Final checks and prompt review-branch shipping are recorded in the completion log. Durable move-history/recovery, Apple execution and the broader lifecycle audit remain open.
- [ ] **desktop-main:R70 — Dock/taskbar new-mail badges:** show an unread Inbox count on the Shep launcher/dock/taskbar, including the GNOME-style badge in the user's screenshot where the desktop supports it. Reconcile new arrivals, read/unread, optimistic moves/deletes/Undo, account scope and restart without stale counts; allow disabling badges in Preferences. Use native platform integration with isolated automated tests and document actual desktop/platform support.
  Linux Unity/Dash-to-Dock protocol adapter and searchable preference are implemented with a coalescing background worker and real private-bus tests. Global count projection observes pending messages outside the current page. All 108 native scenarios, 313 Rust tests and 29 Python tests pass; release packaging is verified. Installed and shipped in 90776fa; evidence is recorded in the completion log. Keep R70 open for Windows/macOS adapters and execution, actual desktop rendering review, and remaining ambiguous-provider/restart reconciliation alongside R50/R60.

- [ ] **R63 — E2E coverage audit:** primary/secondary clear controls, remapping, capture cancellation and persistence are shipped in 742b21e. Continue mapping every remaining visible feature to realistic native happy/error/recovery scenarios as the TODO features land; complete a final functionality-path coverage audit. The current shared-reader verification observed intermittent native input misses during concurrent builds (initial divider drag and rapid Archive/Delete shortcuts). The isolated unchanged scenarios pass; the divider scenario now settles its first frame. Keep rapid-input reproducibility/diagnosis open and preserve the failed evidence; do not describe the two 57/58 full runs as clean suite passes. Browser continuation: the group-removal regression run observed one Print click completing without a popup, with the formatted iframe focused; its unchanged focused rerun passes. The trace remains under artifacts/browser-group-removal-failures/full-first; the subsequent deterministic reader regression and fix are recorded below. Keep the wider input-lifecycle audit open. Do not hide it with a forced click, direct controller call or relaxed timeout. Trace screenshots confirm that asynchronous attachment/HTML preparation moved Print after the pointer was positioned, replacing its old position with the formatted frame. The browser now uses a stable reader action footer and retains its controls and ancestors through same-message redraws. Saved real-input scenarios verify pointer position, a held press through loading, keyboard focus and scrolling through delayed preparation. Final regression and prompt shipping evidence are recorded in the completion log. Preserve the persistent sandbox, text selection and navigation. The keyboard regression also found the default Enter reader shortcut consuming native button activation. Unmodified Enter/Space now activate focused controls; row Flag stays native and opening a focused message honors the configured/disabled reader shortcut. Continue the dependent browser group controls and optimistic queries, alongside the wider list/control press-lifecycle audit. The Flutter footer regression found an obsolete Android Outbox assertion that expected read-on-open. The corrected native scenario now verifies unread while the reopened reader stays open, read after actual Back, persisted native flags and no credential access; preserve this current read-on-leave requirement. The Flutter browser Find regression also observed replacement Find text being appended after switching to plain text; the saved helper now observes native text-editor focus/selection before typing; retain the wider rapid-input audit.

- [ ] **R50/R60 — Immediate feedback:** archive/move, flags, read/unread and other reversible actions show their expected result immediately, before network/SQLite completion. Archiving removes the mail from the current list immediately; restore it with an actionable error on failure. This is the app-wide optimistic interaction principle, recorded in AGENTS.md. Commit 742b21e ships immediate flags/read and same-account moves, coalescing, stale results and rollback with unit/protocol/native verification. Commit d3a530a additionally keeps metadata actions available while bodies load and rejects stale-body targets. Commit 9c907d2 adds immediate cross-account source feedback, typed completion/rollback and ordering behind source read/flag changes. Remaining: reconcile optimistic membership/counts when switching filtered folders, ambiguous cross-account outcomes, other reversible controls, and durable restart recovery for pending actions. Client checkpoint: the Flutter inbox and open reader share persistent mutation errors with Retry/Dismiss; host and Android controls verify missing/locked-credential rollback. The remaining app-wide audit stays open.
- [ ] **R47 — A. Keep → Inbox:** investigate the reported failed move, verify the actual provider path and destination refresh. Commit 590ab10 ships Inbox labeling, send-queue feedback and a spaced-folder protocol test preserving acknowledgment after logout failure; the personal-account root cause is not yet confirmed.

## Mail reading, search and bulk actions

- [ ] **R38 — Faithful HTML:** upstream desktop commits `1968e37` and `32120b4` now render selectable HTML mail with keyboard navigation scoped to reader focus (evidence `6fed03b`); the committed implementation is now present in the review worktree, verified and shipped in `72ca625`. Complete browser/mobile parity for layout, typography, tables, backgrounds, links and inline images in their intended positions. Keep selectable text, a plain-text option, current remote-image controls and no active email scripts. Use synthetic versions of the supplied examples in tests. Current continuation: share MIME representation selection and a resource-confined HTML document between native Flutter and the browser, retaining table/CSS layout, CID images, plain-text choice, selection/Find, quoted-history policy and safe link handling. MIME selection, independent CID resources and pre-parse nesting protection now have shared/native/WASM and client-control evidence. Browser increment: a shared Rust sanitizer converts bounded inline resources to WebP and prepares a confined document with a fixed display/Find bridge. A separate browser worker and persistent sandboxed frame retain authored tables/styles, selection/Copy, shared Unicode Find, quoted-history controls, plain-text choice and reviewed links. Sender scripts and automatic remote requests remain blocked. Shared native/WASM tests, 26 browser scenarios, 49 Rust HTTPS stages and reviewed light/dark compact captures provide the evidence in the completion log; prompt review-branch shipping is recorded there. Flutter increment: two independent native preparation slots, a persistent system WebView, generation/layout protection and coalesced commands now have Rust, host, Android integration, five native Appium stages and nine Flutter browser stages. Native menu Copy/paste, Find across spans, quotes/plain choice, links, dark mode and preparation retry are verified; screenshots and shipping evidence are in the completion log. Correction: Flutter iOS now targets 14.0 to display the shared WebP inline resources. Remaining: Apple execution, native/Apple and broader authored foreground/background contrast in dark HTML (legacy body background/text defaults are now corrected with shared/browser evidence; retain the wider rendering audit separately from composer accessibility), remote-image preferences/exceptions/loading, reading anchors during remote-image reflow, bounded large-document layout and full desktop parity. Main now also includes the committed forwarding work in 9062dcf / ac515a3; integrate the committed changes deliberately.
- [ ] **R44 — Find in message:** upstream desktop ships Ctrl+F, background searching, visible matches and next/previous navigation in `c266035` (evidence `f5f13c9`). Current worktree adds shared Rust literal Unicode/whitespace search, native Flutter and browser-worker Find controls, visible highlights, case matching, next/previous navigation, quote visibility and stale-result protection. Native Rust, host, Android, six Appium stages and both browser paths pass, including locked-credential/offline controls, reviewed highlights and production artifact inspection. The completion log records this verified text-reader increment and its prompt review-branch shipping. Full browser/mobile HTML/Find lifecycle coverage, bounded large-text layout/search, mobile keymap configurability remain open. The previously independent composition/forward work subsequently shipped in 9062dcf / ac515a3 and now also needs deliberate integration/client parity.
- [ ] **R18/R44 — Fuzzy matching:** use a maintained matching library for inbox and Move search, with relevance ordering and typo tolerance. Regression: an email with body exactly “test” ranks ahead of weaker matches. Keep stable paging and stale-query protection.
- [ ] **R41 — Forward:** desktop shipped in 9062dcf / ac515a3, with retained HTML/CID/attachments, blank recipients/new thread and remappable F. The desktop changes are integrated and verified; add/test equivalent mobile/browser controls, retained content/files and independent draft behavior. Prior checkpoint: complete-source Forward preparation is shared by native Rust and WASM, with exact bytes, per-part MIME types, separate CID scopes, malformed resources, new-thread/stable SMTP identity and bounded-failure tests. All 116 desktop functional flows, 377 root/shared tests and the browser regressions pass; verified packaging and review-branch shipping in `64d4936` are recorded in the completion log. Current increment connects native/browser Forward controls to atomic draft creation, stable retries after a lost acknowledgment and independent editor state. Native version-9 cache migration, retained quote metadata, inline Content-IDs, Outbox recovery ownership and gateway MIME have tests. Actual Android and browser controls cover autosave, file removal/restart, missing credentials, damaged resources, complete long source and late preparation without replacing a newer editor. Browser Forward is remappable and input-safe. See the completion log for regression, visual and prompt shipping evidence. Remaining: Apple execution, full composition/multiple-editor parity, large-mail bounds and live-provider verification; keep R41 open for these.
- [ ] **R41 — Print:** desktop now ships isolated browser-preview printing in `b120721` / `c800256`. The desktop changes are integrated and verified; implement/test equivalent browser and mobile print controls. Current increment connects shared complete-source documents to browser previews and Android native printer/PDF controls. Cached aliases, independent provider/reader capacity, complete quoted text, headers/filenames, inline images, cancellation/retry and actual paginated output have native/WASM/host/browser/Android evidence; browser Print is remappable and input-safe. UIKit adapter is added but uncompiled/unexecuted here. See the completion log for verification and prompt review-branch shipping. Remaining: Apple and other browser-engine execution, native keymap parity, permitted cached remote images, lifecycle/large-document and performance audit. Keep current unrelated main-worktree edits untouched. Upstream has since shipped HTML preparation/compact layout and interface scaling in cd8f732 / 6d83b72 (evidence d3b34e7); preserve these during integration and client parity. Main subsequently shipped reading-position preservation during HTML image reflow in cc38af9 / 509e444; keep that behavior during integration and client work. Its latest independent Linux dock-badge work is 90776fa / 3c7acc6; preserve it during integration and track mobile notification/badge equivalents. Main’s desktop-main:R70 identifier is distinct from this thread’s promo-website R70. Main also shipped software text clipping in 0d2feb1 / f99009a; preserve it during integration.
- [ ] **R42 — Multi-selection:** upstream desktop shipped bulk selection and durable Undo in `af056a9` (evidence `10d8569`); the committed implementation is now present in the review worktree, verified and shipped in `72ca625`; client parity remains open. Ctrl+A for all emails in the focused list, Ctrl-click toggles, Shift-click ranges, and a Select button beside conversation search for checkbox selection. Preserve scope across pages and distinguish list focus from text selection. Current prerequisite adds captured query membership in a dedicated native SQLite connection and a bounded Dart controller; it is not yet connected to the mail controls. Rust/host tests cover 100,000-message capture, bounded observations, cross-page ranges, frozen reviews, aliases, explicit arrivals, FIFO/backpressure, cancellation, stale observations and release/retry. Continue replacing loaded-row UI selection with this captured scope. Browser correction from real concurrency tests: do not keep temporary selection membership in the mail IndexedDB database; Chromium serializes even disjoint write transactions and blocks draft saves. Use worker-owned temporary SQLite with read-only mailbox snapshots and a bounded cache-change journal for flags, aliases and account removal. The browser prerequisite now implements exact capture/freeze, a 32-request worker queue, at most 50 observed/review rows, tab isolation and migration preserving acknowledged Sent roles. Chromium tests cover 100,000 messages while draft saving remains available, journal overflow, aliases/current metadata, missing/account removal and failed recapture rollback. The list and selection share the folder/filter/search predicate and stable identity ties. Storage regression/shipping evidence is recorded in the completion log. The browser now connects a bounded gesture controller to Select/Select all/Clear/Done, checkbox and Ctrl/Shift/Mod+A inputs, page/refresh/alias observations and scope resets. Saved Chromium scenarios exercise 125 cached messages, worker startup failure/retry and abandoned pending capture; controller tests cover queue bounds, lost replies and stale observations. Both Dart and browser controllers retain deselection while Select all is pending and a row scrolls away, and acknowledge a range target before its rank arrives. Keep focused-list shortcuts separate from text fields, sidebar and reader text; selection gestures must never read mail or dispatch a single-reader action. Verify optimistic queued intent, failure/retry, compact layouts and actual production worker controls. Flutter control replacement depends on exact durable group execution; do not promote its old loaded-row action loops to full-mailbox selection. Interrupted workers lose their temporary capture; durable bulk jobs remain a separate requirement. Remaining: native Select/Done/Clear/Select all and long-press/checkbox wiring, full native/browser review and durable bulk execution, actual group-action failure/Undo/history controls, Apple/other browser engines and idle-host performance. Preserve the browser modifier/checkbox, text/reader isolation, delayed typing and worker recovery regressions. Keep selection gestures separate from deliberate reading and text selection; preserve queued intent and visible failure/retry. Verify large/multiple-page captures, new arrivals, explicit recapture, stale results and native/browser controls before claiming parity. Reviewed durable bulk jobs remain a dependent active requirement.
  Imported desktop-main remaining work: Native controls, modifier snapshots, list/text focus isolation, cross-page ranges and Select All recapture are connected. The range anchor survives Select All. Finish explicit checkbox/Ctrl-click selection of an arrival outside an existing capture without discarding prior membership; background arrivals must not join silently. The current core passes 347 Rust tests and all 116 native scenarios, with an optimized Linux install; evidence is in the completion log.
- [ ] **R42 — Bulk actions:** preview toolbar and keybinds act on selected messages. Review multi-message actions with Y/N/Enter/Escape support, accurate scope/count and clear partial-failure handling. Current browser prerequisite implements exact frozen export into a separate per-profile durable journal in bounded batches, with interrupted staging excluded from execution, revision-checked decisions, exclusive tab ownership, retained receipts and conservative restart recovery. Eight real Chromium storage scenarios verify 100,000-message export, independent draft saving, restart/tab loss, Undo/retry, bounds and atomic failure; the completion log records the full regressions and prompt shipping. The browser now has a tested provider executor API; application startup and visible group controls are not connected yet; do not replace Flutter’s loaded-row action loops with full captures until the dependent execution path is ready. Optimistic query effects and actual review/History/Undo controls remain required; group account-removal integration now has targeted storage/control evidence below; journal storage alone does not establish bulk parity. Current integration work connects typed acknowledged mutation receipts before fallible cache/display refreshes, protects physical source identities, and coordinates the journal with reviewed execution, recovery and account lifecycle; do not expose approval controls until those invariants hold. Preserve newer individual intent and exact captured membership throughout. Current continuation adds a persistent per-field intent clock to browser storage, reserves individual intent at input time before older queued work completes, keeps it through aliases/sync, and includes pending intent in account-removal review/cleanup. Use these owned revisions for bulk dispatch and Undo; equal flag values alone do not prove that an older group still owns the field.
  Browser individual controls now reserve persistent revisions before older queued work, recheck per-field ownership at dispatch, retire unsent cancellations and preserve acknowledged results through intent-record failures. Schema/alias/account-removal integration and saved real browser controls have targeted evidence; final regression and prompt shipping are recorded in the completion log. Current continuation connects exact journal membership to a single owned provider executor, persists approved/Undo intent revisions, records accepted fields and receipts before cache work, and repairs acknowledged cache gaps without repeating server mutations or overwriting newer intent. Remaining browser work includes visible group approval/execution/Undo, explicit unknown-outcome recovery, optimistic background queries and remaining lifecycle audit. Do not replay pending intents automatically on restart. Correction: mail schema 7 must close older writer connections before cache-applied revision guarantees are used; preserve existing clocks and never infer cache success from an acknowledgment-only status. Current continuation adds group-aware account-removal counts and stale-review protection, group ownership around the authoritative committed mail-removal fence, bounded cleanup of the removed account’s entries and related receipts, and crash recovery driven by the committed mail-account tombstone. Keep unfinished group changes explicit in Preferences and prevent queued work or stale frozen captures from recreating a removed account. Ten saved Chromium scenarios now verify stale/discard reviews, cross-tab ownership, lost mail replies, bounded cleanup/rollback, tab loss, completed/uncertain/inverse/missing entries, source/destination migration and actual light/dark Preferences controls with visible cleanup failures. Mail schema 8 fences older removal code; journal schema 4 waits for an older receipt owner before upgrading. Full regression and prompt review-branch shipping are recorded in the completion log. Visible group review/History/Undo, optimistic query projection, explicit ambiguity resolution and native parity remain open. Current continuation replaces the browser main-thread full-mail/body load with a per-profile SQLite/OPFS query index in a bounded worker. Coordinate each index lease across tabs and release OPFS handles between requests; treat IndexedDB mail, aliases, account tombstones and its change revision as authoritative, rebuilding an absent/stale derived index. Return at most 50 metadata rows and load bounded individual bodies separately. Preserve current folder/Sent/search predicates, independent draft saving, pending individual projections, stale-query/body rejection, account removal and actual list/reader/selection controls. Checkpoint `3dd0837` ships the worker prerequisite; final verification and limitations are in the completion log. Six real Chromium storage scenarios pass, covering 100,000-message cardinality and concurrent draft saving, rollback/replay, cross-tab and abrupt-worker recovery, queue bounds, migration and exact search edge cases; the prerequisite was not yet connected at that checkpoint. Checkpoint `2cb6a46` activates paged list/reader controls; its verification and shipping are recorded in the completion log. Finish bulk projections/recovery before enabling group actions. Current integration connects this path to the real Gateway/Workspace/UI: coalesce page requests, retain metadata actions and counted Undo, project current pending fields, read bodies through independent worker capacity, and keep at most eight prefetched bodies/32 MiB. Startup/list reload body scans are replaced; preserve explicit query/body Retry and saved actual paging, search, selection, delayed actions, rollback, aliases and account-removal tests. Shutdown rejects late worker recreation, and old mutation results cannot update a replacement cache with reused IDs. Provider sync/Outbox bounds remain separately audited; do not claim whole-client bounded memory from page limits alone. The next successful query removes deleted accounts from the derived index; retain explicit physical cleanup/retry/lifecycle work and independent body capacity during a rebuild; full fuzzy ranking, bulk projections, provider sync/Outbox bounds and final performance remain active requirements. Current continuation activates browser group controls end to end: freeze exact membership for an action review, approve/pause/resume with durable ownership, project full-group mail membership/counts without transferring all IDs to the UI, expose bounded History/item pages, immediate group Undo and explicit failed/uncertain/cache-recovery decisions. Coordinate query/selection snapshots with journal revisions and per-field intent so stale progress or newer individual choices cannot be overwritten. Preserve restart/account-removal fences and test real controls with delayed/failing providers, off-page membership, mixed accounts, rapid Undo, restart and reviewed light/dark layouts. Keep native equivalents and unsupported cross-account transport explicit; do not enable a loaded-row group loop. Browser group review/execution, full-group projection, bounded History, Pause/Resume, Undo and explicit failure/uncertainty controls now have targeted Chromium evidence; final regression and prompt shipping are recorded in the completion log. Keep synchronous Undo row/count restoration before local decision persistence, startup recovery notifications, abandoned-review cleanup, staging/alias/overlapping-group lifecycle audit, cross-account transport, large-group performance and native equivalents open.
  Frozen durable jobs, bounded/coalescing execution, optimistic query effects and weighted feedback, per-message History, grouped Undo/retry, graceful stop/resume and account-removal cleanup are implemented in the integrated Rust desktop. There are 347 passing Rust tests, including receipt/phase ordering, independent-process exclusion, queued pointer input and close-at-entry recovery. All 116 native functional flows pass, including the four bulk flows and three selection flows; the optimized release package and Linux installation are verified. Complete group/individual mutation coordination and the remaining native History/recovery paths before closing this request; ambiguity across providers remains tracked under R50/R60.
- [ ] **R45 — Drag mail to folders:** drag one mail or a selected group onto sidebar folders; highlight valid destinations, respect cross-account preference/provider support, and use the bulk-move confirmation.

## Folders and composition

- [ ] **R30 — Folder trees:** nested folders form collapsible groups, collapsed by default, honoring each IMAP hierarchy delimiter and nonselectable parents.
- [ ] **R30 — Folder context menus:** delete folders and move them inside another folder; review destructive scope, keep account/server/cache state consistent, and test failure/retry behavior.
- [ ] **R35 — Inline composer:** new messages/replies open in the preview pane; autosave while typing and support switching among multiple drafts and received mail without losing content, recipients or attachments. Browser regression found during read-on-leave verification: multiple saved drafts inherit the three-column mail/5 px divider grid, making one draft overlap another. Saved drafts now have an independently scrollable, clickable list with correct counts; compact Playwright controls reopen every item and the real HTTPS account-removal/draft regression passes. This does not complete inline/multiple-editor parity.

## Account/settings sync and backups

- [ ] **R76 — Scheduled out-of-office replies:** add **Preferences → Automatic replies** for one account, selected account groups or all accounts. Use a list of named reply entries: type the message once, set its schedule, then choose accounts with a searchable multi-select, saved account groups and **Select all**. Add another entry for a different message/group without retyping the first (for example, Work and Personal). Include start/end date and time, a visible timezone, clear active/upcoming/ended status, and easy edit/disable controls. Validate end-after-start and resolve overlapping assignments explicitly. Implement and show provider support/setup and per-account results; unsupported or failed accounts must never appear enabled. Prefer server-side scheduling so replies work while Shep is closed. Cover account/group assignment, scheduling boundaries, timezone handling, overlapping edits, partial failure/retry and client parity in provider/UI/E2E tests.

- [ ] **R75 — Sign in with Google:** replace manual Google token entry with an OAuth **“Sign in with Google”** flow across desktop, Flutter and browser clients. Audit existing Google setup screens and distinguish application OAuth configuration from user access/refresh tokens. Request only the scopes needed by enabled features, keep secure token refresh/revocation and account switching, and test denied/cancelled/expired grants. The browser beta gate alone does not connect Gmail, Calendar or Drive; keep provider access and live verification explicit.

- [ ] **R02/R49 — Continuous Drive sync:** use OAuth and private appDataFolder for accounts and portable preferences. Offer opt-in on the first Google setup; discover an existing setup automatically on another PC; provide an ongoing toggle. Sync appearance, shortcuts, contacts and other portable settings with conflict-safe updates and offline recovery. Keep machine-specific paths/window placement local.
- [ ] **R49 — Account credentials on new PCs:** complete the password-protection decision and secure import into the OS keychain. An asynchronous question asks whether to use an additional sync passphrase (once per new PC) or Google-only protection; no answer has arrived yet. Do not claim password sync is implemented.
- [ ] **R32 — Multiple backup destinations:** polished destination list/setup for Local, Google Drive, S3, FTP/FTPS and SFTP, with independently configurable connections and easy extension points.
- [ ] **R32 — Backup options:** compression and chosen passcode encryption at setup, password prompt on restore, multiple destinations enabled together, independent upload results and configurable per-destination rolling copies (e.g. ten unreadable encrypted archives).

## Storage, appearance and final quality

- [ ] **R64 — GNOME launcher icon:** install the approved dog mark with a transparent background, matching the system theme; verify desktop/dash integration and theme changes without replacing the approved design. There are copies of the transparent icon in the shep.website folder one level up from the root
  Current sibling checkout is `../shep-website`; the inspected `assets/`, `docs/images/` and `website/dist/assets/` logo copies are opaque, as are `../shep-clients/web/public/` copies. Locate the intended alpha variants or remove only the background from the approved design.
  Built-in imagegen cutouts and their prompts are saved under ignored `artifacts/imagegen/launcher-alpha/`. The dark results have ragged edges and were rejected; the light candidate needs exact-silhouette review. No candidate has replaced the approved installed assets. Finish clean extraction before theme integration.

- [ ] **R61 — Download-and-install scripts:** provide directly runnable GitHub raw installer scripts for Linux, macOS and Windows. Default to the user home/equivalent, optionally prompt for all-user/system installation with appropriate elevation, and integrate with GNOME/KDE applications, macOS Applications and Windows Start menu. Put the commands near the top of README as the first install option and in the install docs; test download/extraction/install/update and cancellation using isolated fixtures.

- [ ] **R22 — Encrypted local cache:** encrypt SQLite/mail/attachment data at rest and safely migrate existing personal data; keychain credentials and encrypted backups alone do not fulfill this request.
- [ ] **R23 — Large mail:** remove the incoming 25 MiB, snapshot 256 MiB and preview 32,000-character ceilings using bounded streaming/paging and background large downloads. Large mail must not hold up smaller messages. Avoid replacement arbitrary caps/unbounded RAM allocations. Current reader prerequisite: validate multipart nesting iteratively before invoking the recursive MIME parser, and share representation/CID selection between native Rust and browser WASM. A defensive nesting check does not remove the existing size limits or establish streaming/performance parity.
- [ ] **R25 — Palette editor:** configure primary, secondary, background, surface, text, accent and related theme colors in Preferences, with persistent light/dark schemes and readable states.
- [ ] **R15/R17/R21 — Final usability review:** finish remaining redundant-copy, spacing, icon sizing, alignment and compact/large-font checks, Singular message-count labels are corrected in the HTML-reader work; finish the remaining visual review. Preserve the approved dog logo, WebP assets, mouse usability, draggable panes and clean shadcn-style controls.
  The residual scale-control fragment below filtered settings is fixed in 0d2feb1, with direct renderer and saved native pixel regressions; all 109 native functional scenarios pass and the release is installed. Continue checking the previously reported clipped Shortcuts label and other compact/large-font states in the remaining visual audit.
- [ ] **R01/R06 — Provider/platform completeness:** close remaining POP3 protocol/lifecycle gaps, Google/CalDAV/SMTP integration evidence, independent-process coordination and Windows/macOS execution/distribution checks. Do not present fixture evidence as live-provider verification.
- [ ] **R03/R09 — Final performance gates:** keep measurements deferred while the host is busy; run backend/native gates at the end on an idle host. Do not weaken budgets. Keep quality/release CI disabled until requested; documentation publishing has a separately recorded exception in AGENTS.md.
- [ ] **R08/R10 — Ship verified work:** The latest user request repeats prompt shipping of all combined work; validate the pending browser group increment and push the review branch at the next working checkpoint. push at the next opportunity with a working build, as requested; do not wait for the full product backlog to be finished. Run fmt, Clippy, Rust/Python and relevant native tests; optimized release/installer verification; For desktop-main shipping, install for the Linux user and push main at sam-ruff/shep.so. This client worktree ships only to the authorized review branch; do not merge/push these clients to main or replace the installed desktop during this review phase. Keep logs/artifacts out of the root and preserve personal data.
