# Shep TODO

Active unfinished requests. Read [handover.md](handover.md) before resuming. Completed behavior and test evidence live in [docs/COMPLETION.md](docs/COMPLETION.md); [docs/REQUEST_AUDIT.md](docs/REQUEST_AUDIT.md) preserves every original request. Add requests immediately, and remove them only after relevant verification and shipping. The full product goal remains unfinished.

## Next priority — portability and shared profiles

- [ ] **R92 follow-up — OAuth implementation handover:** implement Google OAuth and cross-device account/profile sync as the top priority, following [the Flutter interoperability handover](https://github.com/sam-ruff/shep.so/blob/feat/mobile-web-clients/docs/agents/PROFILE_SYNC_HANDOVER.md). Deliver first-device setup, new-device discovery and ongoing configurable sync using the shared client format. Both restart notes and the client TODO link to this contract; working profile sync and its verification remain open.

- [ ] **R92 — Rust/Flutter profile interoperability:** full database export/import and local profiles (R83) are shipped in `93d4289`; next implement Google OAuth/Drive account/profile sync (R02/R49). Support creating the first shared profile from either desktop or Flutter, discovering/importing existing profiles on a new device, keeping already connected devices updated, adding/removing accounts and offline/reconnect/conflict handling. All profile/sync choices must be configurable and toggleable. The inspected integration points and proposed shared-format/OAuth/credential/enrollment/testing contract are shipped in `shep-clients` as `02c4b32`, with audit `59578f3`; use its current shared profile-core format and client consent work rather than rewriting the handover. Keep account passwords out of unprotected exports/sync files; confirm the outstanding credential-transfer protection choice.

## Mail, folders and desktop integration

- [ ] **R91 — Channel-owned backend state:** use bounded channels and state-owning workers for coordination, rather than shared lock-managed state. Account/calendar scheduling, the mail-cache connection/local leases, profile catalog and OS credentials have bounded owning workers, with ordering, cancellation, draining and failure tests. Database transfer has its own connection/controller and leaves the cache worker available. SQLite was updated after reproducing its Unix WAL open/close deadlock in the notification restart test. Continue lifecycle/Google and backup-journal ownership as those paths change. Channels must keep UI work nonblocking; do not substitute an unbounded queue. R64 validation exposed one shared-core Journal reopen `Owned` failure after drop; exact unchanged and full-history reruns pass. Investigate possible fork/exec file-lock inheritance without weakening ownership assertions; retain r64 hook/rerun evidence.

- [ ] **R90 — Slow sync/close investigation:** reproduce unexpectedly long synchronization and shutdown, identify whether shutdown waits on unnecessary background work rather than required durable saves/receipts, and fix confirmed causes with deterministic slow-backend and native close/recovery tests. Distinguish live-server delays from local scheduling bugs; preserve actual in-flight write acknowledgments. Coordinate with R86 temporary tray while saving. The channel-coordinator fix interrupts a held read-only download for interactive writes and retains ownership until in-flight cache writes settle; deterministic and native close/read/flag regressions pass. Shutdown continuation, ordered errors, owned attachment/discard/forward failure recovery and interruptible pre-claim provider waits are integrated in `2733cc6`; 32 targeted tests and mandatory hooks pass. All 23 selected native scenarios pass on the integrated binary, and this source is pushed to main. Finish native/temporary tray handling and remaining personal-account diagnosis; the source checkpoint is not installed yet.

- [ ] **R88 — Compact email list and unread styling:** remove sender avatars/initial icons from the inbox/conversation preview list and reduce each row’s height, following the supplied compact horizontal example (sender, subject, snippet and time). Retain Shep’s action buttons, selection controls and usable mouse targets. Unread mail should have a subtle background highlight, a dot indicator and a bold subject; keep unread styling distinguishable from selected/hovered rows in light and dark themes. Implemented in `codex/compact-mail`: shared 60-pixel geometry, measured ellipsis, scrollbar gutter and 40×44 flag/selection targets. Eight layout/control/deletion scenarios and 20 migrated selection/context/move/drag scenarios pass; light/dark/wide/compact WebPs reviewed. Root integration now uses actual native viewport bounds and tests full-row visibility at 120%/compact size. All 248 native paths have passing coverage across the complete run and the corrected picker/last-row setup reruns; all 81 Python and Windows GNU checks pass. Main publication and its receipt remain pending.


- [ ] **R86 — Native close-to-tray and temporary saving tray:** closing the main window should hide Shep in the native system tray/menu bar, with Restore/Open and explicit Quit actions. Keep background mail sync and notifications working, provide a preference, and handle desktops without tray support gracefully. Cover Linux/Windows/macOS behavior and preserve pending drafts/operations on actual Quit. Follow-up: even with close-to-tray disabled, closing while saves are pending should temporarily hide to the native tray, notify that Shep will quit once saving finishes, and then exit automatically. Preserve failure recovery and allow reopening; do not silently lose drafts or pending writes. The original tray request was backlog-only during the handover; this follow-up also asks to investigate excessive sync/close delays. Active parallel lane `codex/native-tray` is implementing the iced daemon/window lifecycle, Linux StatusNotifier adapter, Windows/macOS native menu adapters and deterministic/native host tests; implementation includes native Open/Quit, temporary saving notices, window reopening and an isolated native tray harness. Nine targeted Rust, 58 Python and all 26 selected native scenarios pass with reviewed WebPs, full Windows GNU checking and exact macOS adapter checking. Mandatory hooks and root integration are underway; actual Windows/macOS execution, full macOS app checking and production desktop review remain pending.
  Tray source `6728931` is pushed with 745 hook executions and 32 integrated native scenarios passing. Root reproduced hidden failed sends after ordinary close-to-tray. The correction is pushed in b8eafde:12 tray/controller tests,748 hook executions and16 selected native scenarios pass, including Quit from an already-hidden window, older-result ownership and queue backpressure. Actual platform execution and remaining personal-server diagnosis stay open.

- [ ] **R30 — Folder context menus:** reviewed Move/Delete, nested groups and recovery controls shipped in 3567de2. Finish combined-folder optimistic scopes, aggregate common-folder account choice, history paging/account removal and cache convergence after accepted uncertainty. Keep original mail when an outcome is unconfirmed; acceptance is not server success.
- [ ] **R73 — Moved mail missing from destination:** recovery/projection checkpoint a81d767 is shipped. Finish actual adapter wire/journal integration, broader Undo/bulk/folder-history lifecycle and repeated-move aliases, plus independent-process coordination with R01.
- [ ] **R47 — A. Keep → Inbox:** confirm the personal-account failure's root cause and actual destination refresh. Inbox labels and spaced-folder protocol checks are shipped; live diagnosis remains open.
- [ ] **R50/R60 — Optimistic interaction everywhere:** immediate flags/read/move/Undo feedback is shipped. Finish membership/count reconciliation when switching filtered or combined folders, ambiguous cross-account outcomes, other reversible controls and durable restart recovery. Preserve newer intent and visible rollback errors.
- [ ] **R82 — Native new-mail notifications:** default-on popup/sound/details, independent preferences and isolated Linux/native fixtures shipped in ded5aca. Finish actual Windows/macOS delivery, macOS bundle integration and real desktop popup/sound review. Initial imports remain quiet; fixture success is not live desktop verification.
- [ ] **R70 — Dock/taskbar badges:** Linux Unity/Dash-to-Dock adapter and preference shipped in 90776fa. Windows taskbar overlays and native macOS Dock labels are integrated/pushed in 1595fb3, using bounded count delivery and shell/window lifecycle recovery. The integrated source passes 751 hook executions, 59 Python tests, 12 native Linux scenarios, full Windows GNU checking and exact macOS adapter checking. Actual Windows/macOS execution, desktop rendering and ambiguous-provider/restart count reconciliation remain open.

## Accounts, portability and backups

- [ ] **R02/R49 — Continuous Drive sync:** OAuth/appDataFolder account definitions and portable preferences, first-device opt-in, existing-setup discovery on another PC, ongoing toggle, conflict-safe changes and offline recovery. Keep local paths/window placement device-specific.
  Native creation/recovery, reviewed import, eight portable settings and post-login discovery/enrollment are shipped in `acb4969`. One complete profile imports automatically into an untouched workspace; Not now persists opt-out and populated devices retain review. Common-value/local-edit checkpoints are durable. Ongoing publication/application is shipped in `bd50c52`; conservative reviews and further interoperability work remain below. See [completion](docs/COMPLETION.md) for the 30 native scenarios and shipping evidence.
  The current cycle admits local edits before pulls, applies safe remote preferences/account names/new definitions, and preserves deferred conflicts plus dirty native preference fields. Four new native paths cover automatic receipt, local publication, offline retry and partial failure; they pass on the integrated `d29ce06` binary.
  Durable native edit generations now preserve setting/account-name reversions through pulls, acknowledgments and restart. Next finish account linking/suppression on populated devices, incremental polling, conflict/removal/endpoint reviews, already-admitted legacy recovery, password-prompted legacy backup migration and remaining portable settings. Preserve field bases when edits race pulls and merge dirty UI preferences per field. Verify actual cross-client Google access; keep active client work intact.
  Verified immutable history caching is shipped in `e77eabc`, with 80 profile tests and nine integrated native scenarios passing. Full incremental change-token polling remains separate follow-up work.
  Portable-setting conflict reviews are implemented in `codex/profile-reviews`: bounded scalar choices, exact reviewed version sets, local/native generation checks and durable resolution requests. Six targeted store/history/controller regressions and 86 matching profile tests pass; 25 native profile flows pass with reviewed light/compact-dark/error feedback. The integrated binary also passes all 25 native profile flows plus 81 Python tests; source is pushed as `22a3af5`, with 762 root hook executions passing. Existing endpoint/removal and account-link reviews remain separate unfinished work.
- [ ] **R49 — Portable account credentials:** decide passphrase-protected transfer versus Google-only protection, then implement secure keychain import. No answer to the earlier protection question was recorded; do not claim password sync works.
- [ ] **R32 — Multiple backup destinations and options:** polished Local/Drive/S3/FTP/FTPS/SFTP setup with extensible providers, multiple destinations enabled together, per-destination results and rolling retention, optional compression and chosen-passcode encryption, and password-prompted restore. Existing single Local/Drive support does not complete this. Parallel Local/Drive checkpoint adds named destinations, independent schedules/history/passphrases/retention and reviewed removal, with migration/restart/alias and native compact-layout checks. Local/Drive source is pushed as `2b87db4`, with 756 hook executions, ten preference tests and 21 integrated native scenarios passing. S3/FTP/FTPS/SFTP, optional compression/encryption controls, combined manual backup and richer per-destination failure history remain unfinished.

## Storage, appearance and installation

- [ ] **R22 — Encrypted local cache:** encrypt SQLite/mail/attachment data at rest and migrate existing personal data safely. Keychain credentials and encrypted backups alone do not fulfill this.
- [ ] **R23 — Large mail:** remove incoming 25 MiB, snapshot 256 MiB and preview 32,000-character ceilings through bounded streaming/paging and background large downloads that do not hold up small mail. Avoid replacement arbitrary caps or unbounded RAM; audit outgoing/attachment limits too.
- [ ] **R25 — Palette editor:** persistent light/dark primary, secondary, background, surface, text, accent and related colors, with readable controls in Preferences.
- [ ] **R64 — Transparent theme-matching launcher icon:** clean approved-design light/dark traced vectors and transparent WebP/PNG, Linux native symbolic launcher/tray matching and macOS template source are implemented in `codex/native-tray`. Original references are preserved; rejected imagegen edges were not shipped. Three targeted Rust, all 81 Python, full Windows GNU and exact macOS adapter checks pass; nine native MCP flows pass with reviewed light/dark/compact WebPs and actual X11/SNI identity. Strict docs, mandatory hooks and root integration/shipping are underway. Personal installation plus actual GNOME/dash and Windows/macOS shell review remain open; GTK fixture evidence is distinct from those desktops.
- [ ] **R61 — Direct download installers:** GitHub raw scripts for Linux/macOS/Windows, home-directory installation by default, optional all-user/elevated installation, native application-menu integration and isolated download/extraction/update/cancellation tests. These are now the first install options in README and install docs. Linux `3676dad`, macOS `0f2af1f` and Windows `ce4a2a6` are pushed with normal hooks and green docs CI. All 81 Python tests pass, including actual PowerShell execution with explicit native boundary fixtures on Linux. Actual OS execution and published-asset installation remain distinct from fixture verification; binary releases are not published yet.
- [ ] **R15/R17/R21 — Final usability review:** finish redundant-copy, spacing, icon sizing/alignment, compact/large-font and clipped Shortcuts-label checks. Preserve mouse usability, saved draggable panes, WebP assets and the approved design. Filtered-settings renderer correction 0d2feb1 is shipped.

## Verification and shipping

- [ ] **R93 — Parallel delivery:** use multiple agents in isolated Git worktrees to progress independent TODO features concurrently. The primary agent integrates tested commits into `main`, resolves conflicts and runs combined regressions before pushing. Run isolated correctness-only native flows alongside capped unrelated builds; coordinate quiet pixel/latency measurements and idle-host performance windows. Preserve all existing requests and record each lane's evidence and limitations. The request to finish every TODO supersedes earlier backlog-only staging, without waiving tests or shipping requirements.

- [ ] **R63 — Final E2E functionality-path audit:** map every visible feature to realistic native happy/error/recovery scenarios as remaining features land. Remapping/clear controls and rapid native keyboard ordering are shipped; preserve deliberate rapid-input tests without extra waits. Investigate intermittent GTK file-picker startup/clipboard readiness: one integrated export setup failed with an unpainted picker, followed by three unchanged passing reruns; evidence is retained under `artifacts/e2e/ee7f4ccad7e2`. The integrated 248-flow run repeated the unpainted GTK picker failure (`1a953a52acb2`); test an isolated Cairo-rendered picker without changing production rendering, clipboard ownership proof or timeouts. The cross-page deletion scenario must await actual last-row reveal before capturing the scroll offset it checks after deletion.
- [ ] **R01/R06 — Provider/platform completeness:** finish POP3 malformed/duplicate UIDL and lifecycle gaps, rejected-command handling in remaining async-imap helpers, Google/CalDAV/SMTP integration evidence, independent-process write coordination and actual Windows/macOS execution/distribution. Distinguish fixture from live evidence.
- [ ] **R03/R09 — Final performance gates:** measure only at the end on an idle host; do not weaken budgets. Keep quality/release CI disabled until requested. Documentation CI has a separately authorized exception.
- [ ] **R08/R10 — Ship remaining verified work:** working checkpoints may push directly to `sam-ruff/shep.so` main. Run applicable hooks/Rust/Python/native checks and release/installer verification before installing production updates. Keep logs under ignored artifacts and preserve personal data. The handover push is a source checkpoint, not a new installed release.
