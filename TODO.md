# Shep TODO

Active requests from this chat. Add new requests here immediately, including corrections. Keep an item until implementation and relevant automated/native verification are complete; remove it only while recording its evidence in [the completion log](docs/COMPLETION.md). [The request audit](docs/REQUEST_AUDIT.md) maps the complete conversation, including work already delivered. The full product goal is still active. Start with [handover.md](handover.md) before resuming; this cleanup removes repeated history, not unfinished requirements.

## Top priority — OAuth and shared profiles

Flutter and desktop now have durable discovery, reviewed initial publication and account/preferences enrollment using independent histories. Desktop enrollment code [`8f969cd`](https://github.com/sam-ruff/shep.so/commit/8f969cd91d45ac2e4a821c927c360a86e64569cd) is pushed and remotely verified: 470 Rust tests, 125 native functional flows and strict docs pass. See [the completion log](docs/COMPLETION.md), [desktop contract](docs/agents/PROFILE_DESKTOP.md) and [Flutter enrollment contract](docs/agents/PROFILE_ENROLLMENT.md) for evidence and limits. **Current work:** native large-page controls; ongoing Flutter/desktop reconciliation and automatic restoration/first setup; complete categories/settings and authenticated cross-client interchange. Preserve tracked upload identities, reconnect guards and explicit credential review. Incomplete discovery or a different Google project's app-data space must never imply empty setup.

- [ ] **desktop-main:R92 / R75 / R02 / R49 — Implement OAuth profile sync:** follow [the interoperability handover](docs/agents/PROFILE_SYNC_HANDOVER.md). Cover first setup originating on Rust desktop or Flutter, discovering existing profiles on new devices, continuing sync on enrolled devices, profile/category toggles, offline edits/conflicts, account removal and secure credential import. Coordinate complete database transfer with desktop-main:R83. Keep this first until delivery. Shared codec/history, scoped desktop/Flutter consent and durable Drive discovery are implemented prerequisites; their tests and commits remain in [the completion log](docs/COMPLETION.md). **Remaining:** browser publication/enrollment and first-setup onboarding, automatic desktop setup and ongoing Flutter/desktop reconciliation, all portable settings/categories, browser consent, automatic native SDK restoration, safe account switching and verified same-project cross-client access. Preserve the active grant after denied consent and require reviewed switching where the SDK supports only one account. The account-password protection choice is unanswered. Passing fixtures, discovery or a pushed handover do not establish working continuous sync or credential portability.

## Mobile, web and website (same monorepo)

- [ ] **R67 — Flutter parity:** Keep Rust desktop at the root, Android/iOS in `flutter/`, the separate iced-style browser in `web/`, and promo in `website/`; the separate `shep.flutter` repository and hosted Flutter layout are superseded. Preserve the shadcn theme, approved logo, configurability and complete mail/calendar/provider/backup behavior. Shared MIME, drafts, attachments, Forward/Print, account removal, native credential handover, Sent identity and optimistic action foundations have recorded host/Android/browser evidence. **Remaining:** full composition/multiple editors, native/browser Sent labels/history, browser Outbox paging, bounded body/cache/large-MIME work, account editing with reviewed identity migration and browser credential handover, calendar/Google/backups, latest desktop defaults, native keymaps, complete lifecycle/accessibility/performance audit and Apple/live-provider execution. SQLite is still unencrypted. See the completion log and parity matrix; fixture success is not full parity.

- [ ] **R68 — Mobile mail interactions:** K-9-inspired sender/subject/snippet rows, account/unread/attachment indicators and swipe action icons; independently configurable left/right swipes, immediate feedback, undo/failure recovery, accessible clickable equivalents and persistent preview settings. Continuation: shared acknowledged IMAP receipts/exact-content lookup, stable client identities, durable unresolved intents and queued/post-refresh Undo now have protocol/cache/browser/Android evidence. Finish explicit review/retry for ambiguous or unrecoverable moves, cross-account behavior and the complete accessibility/provider-success audit; do not treat this as full move parity.
- [ ] **R69 — Flutter quality/release:** Continue Walkie Textie-style Playwright plus native integration/Appium testing. Preserve existing Android emulator, Flutter browser, formatted-reader, Forward, Print/PDF, account/credential and large-text control regressions. Check the generated HTML fixture against shared Rust preparation before both browser and Android preview builds. The Google Appium flow now selects clickable controls, scrolls through actual touch input and verifies cancellation/retry/disconnect; all seven flows pass, with failed screens/trees retained in the completion log. **Remaining:** Apple simulator execution, comprehensive happy/error/recovery/lifecycle/performance coverage, signed coordinated desktop/mobile/browser releases and distribution. Keep quality/release CI disabled; documentation CI remains enabled.

- [ ] **R70 — Promo website:** Delegated agent source/assets are integrated into the combined review branch: concise copy, synthetic screenshots, user-agent-aware primary installation, all-platform alternatives and honest Android/App Store/native/browser availability. **Remaining:** deploy with the protected browser client and replace unavailable destinations with verified store/release links as distribution becomes available. Do not advertise unpublished clients as installable.

- [ ] **R71 — Browser hosting:** build a separate browser client in `web/` matching Rust iced and deploy at shep.so, also supporting network access. Correction R74: a Rust backend on the email VPS is now authorized for beta verification and IMAP/POP3/SMTP transport; the static-only/local-bridge proposal is superseded. Retain client/provider ownership and no persistent server-side mail/password storage unless explicitly changed. Keep current development in worktrees; obtain VPS/OAuth configuration before deployment. Do not claim browser TCP sockets or preview behavior establish provider parity.
- [ ] **R72 — Linux app stores:** investigate and prepare Flathub and other appropriate Linux distribution/store submissions, packaging, metadata, screenshots and verification; track publication separately.

- [ ] **R73 — Continuous parity:** The AGENTS reminder, docs/CLIENT_PARITY.md, shared scenario matrix and parity checker are implemented. Keep them current for every desktop feature/change: port equivalent mobile/browser behavior or retain an explicit active gap with tests. Browser behavior follows Rust iced; mobile uses the Flutter layout. Continue worktree-only development until integration is requested.

- [ ] **R74 — Restricted browser beta and Rust VPS service:** Rust Google-login verification, fail-closed owner allowlist, session/origin/CSRF protections and authenticated IMAP/POP3/SMTP transport are implemented with isolated production HTTPS evidence. Installation on the email VPS is authorized once the SSH target, exact verified owner identity and Google OAuth configuration are supplied; never infer the identity from Git metadata. Support administrator expansion of the allowlist later. **Remaining:** VPS installation/live verification, secure remembered client credentials, full account/calendar/backup parity, Outbox/cache bounds, move recovery and Sent history/labels. Keep mail/passwords out of persistent server storage and logs; retain client/provider ownership and conservative ambiguous-SMTP recovery. Browser login alone does not connect Gmail/Calendar/Drive.

## Current UI and interaction work

- [ ] **R65 — Background mail freshness:** Preserve the integrated desktop behavior and port later committed main changes deliberately. Browser visible-tab and Flutter foreground checks use 15-second intervals and independent queued manual refresh. Read-on-leave, explicit unread precedence, current pending-field projections and counted six-second Archive/Delete/Move/Restored feedback have client controls. **Remaining:** OS background scheduling, window/tab close and forced-termination durability, broader lifecycle/polling/error retention, Apple execution, large-cache performance and durable move recovery. Preserve newer intent, acknowledged identities, aliases, partial rollback/Undo, account removal and independent browsing while providers are blocked. Main's colliding R67/R68 request numbers are desktop-main:R67/R68; never overwrite this thread's mobile requirements.

- [ ] **desktop-main:R70 — Dock/taskbar new-mail badges:** show an unread Inbox count on the Shep launcher/dock/taskbar, including the GNOME-style badge in the user's screenshot where the desktop supports it. Reconcile new arrivals, read/unread, optimistic moves/deletes/Undo, account scope and restart without stale counts; allow disabling badges in Preferences. Use native platform integration with isolated automated tests and document actual desktop/platform support.
  Linux Unity/Dash-to-Dock protocol adapter and searchable preference are implemented with a coalescing background worker and real private-bus tests. Global count projection observes pending messages outside the current page. All 108 native scenarios, 313 Rust tests and 29 Python tests pass; release packaging is verified. Installed and shipped in 90776fa; evidence is recorded in the completion log. Keep R70 open for Windows/macOS adapters and execution, actual desktop rendering review, and remaining ambiguous-provider/restart reconciliation alongside R50/R60.

- [ ] **R63 — E2E coverage audit:** Continue mapping every visible feature to real native/browser happy/error/recovery controls with saved automated equivalents and reviewed screenshots. Retain held-pointer/footer controls, list/reader/text shortcut isolation, divider settling, read-on-leave and native editor-focus regressions. Browser shortcut capture now preserves held presses/modifiers through background redraws and saves/cancels current settings. History progress no longer waits for Undo preparation; failed previews retain visible Refresh recovery. Final regression passes 133/133 Chromium scenarios and 123 unit tests; verified shipping is recorded in the completion log ([`1ea12a6`](https://github.com/sam-ruff/shep.so/commit/1ea12a677829dcd71c4246b87cae46327f9c749f)). **Remaining:** rapid native input during concurrent builds, wider list/press and full keymap/focus lifecycle, complete native/Apple equivalents and overall feature coverage. Failed-before traces remain in artifacts/browser-capture-failures/. Native index verification also reproduced compact bulk checkbox and badge-preference input failures during compilation; saved screenshots/state and the unchanged no-build rerun are recorded in the completion log. Keep this input-under-load audit open. Never hide failures with forced clicks, direct controller calls or relaxed deadlines.

- [ ] **R50/R60 — Immediate feedback:** archive/move, flags, read/unread and other reversible actions show their expected result immediately, before network/SQLite completion. Archiving removes the mail from the current list immediately; restore it with an actionable error on failure. This is the app-wide optimistic interaction principle, recorded in AGENTS.md. Commit 742b21e ships immediate flags/read and same-account moves, coalescing, stale results and rollback with unit/protocol/native verification. Commit d3a530a additionally keeps metadata actions available while bodies load and rejects stale-body targets. Commit 9c907d2 adds immediate cross-account source feedback, typed completion/rollback and ordering behind source read/flag changes. Remaining: reconcile optimistic membership/counts when switching filtered folders, ambiguous cross-account outcomes, other reversible controls, and durable restart recovery for pending actions. Client checkpoint: the Flutter inbox and open reader share persistent mutation errors with Retry/Dismiss; host and Android controls verify missing/locked-credential rollback. The remaining app-wide audit stays open.
- [ ] **R47 — A. Keep → Inbox:** investigate the reported failed move, verify the actual provider path and destination refresh. Commit 590ab10 ships Inbox labeling, send-queue feedback and a spaced-folder protocol test preserving acknowledgment after logout failure; the personal-account root cause is not yet confirmed.

## Mail reading, search and bulk actions

- [ ] **R38 — Faithful HTML:** Shared Rust/native/WASM preparation, confined browser frames and persistent Flutter WebViews preserve tables/CSS, CID WebP images, plain text, quotes, selection/Copy/Find, reviewed links and retry. Sender scripts and automatic external requests stay blocked; Flutter iOS minimum is 14.0 for shared WebP resources. **Remaining:** Apple execution, native/Apple and broader authored foreground/background contrast in dark mail, remote-image preferences/exceptions/loading, reading anchors during image reflow, bounded large-document layout and complete desktop parity. Use synthetic source examples and retain the existing rendering/control regressions.

- [ ] **R44 — Find in message:** Shared literal Unicode/whitespace search, visible highlights, case options, next/previous navigation, quoted-history scope and stale-result protection have native/host/Android/Appium/browser evidence. **Remaining:** full HTML/Find lifecycle, large-text bounds, mobile keymap configuration. Preserve offline/locked-credential behavior and reviewed highlights.

- [ ] **R18/R44 — Fuzzy matching:** use a maintained matching library for inbox and Move search, with relevance ordering and typo tolerance. Regression: an email with body exactly “test” ranks ahead of weaker matches. Keep stable paging and stale-query protection.
- [ ] **R41 — Forward:** Shared complete-source Forward and actual native/browser controls retain HTML/CID/attachments, blank recipients/new thread, atomic independent drafts, autosave and stable retries after lost acknowledgments. Preserve exact files, removal/restart, damaged-resource recovery, missing-credential refusal and late preparation that cannot replace a newer editor. Browser Forward is remappable/input-safe. **Remaining:** Apple execution, full composition/multiple-editor parity, large-mail bounds and live-provider verification.

- [ ] **R41 — Print:** Shared complete-source documents and actual browser/Android printer/PDF controls cover complete quotes, headers/filenames, inline images, aliases, independent navigation, cancellation/retry and paginated output. Browser Print is remappable/input-safe. **Remaining:** compile/run the UIKit adapter, other browser engines, native keymaps, permitted cached remote images, lifecycle/large-document/performance audit. Preserve later desktop scaling, image-reflow reading position, software clipping and dock-badge behavior during integration; keep main's independent work untouched.

- [ ] **R42 — Multi-selection:** Browser worker SQLite capture/freeze and bounded gestures now drive Select/Select all/Clear/Done, checkboxes, Ctrl/Shift/Mod+A, cross-page ranges, immediate scope reset and recovery. Native SQLite capture and a bounded Dart controller exist, but Flutter still uses loaded-row action controls. **Remaining:** connect native captured membership only alongside exact durable execution; implement native checkbox/long-press/selection controls, reviews, failure/retry, Apple and performance tests. Preserve 50-row observations, one in-flight/32 pending gestures, independent drafts during 100,000-message capture, explicit arrival recapture, range anchors, aliases, stale/lost observations and release. Selection must not read mail or consume text/sidebar/reader shortcuts. Imported desktop gap: explicitly selecting an arrival outside a prior capture must preserve the existing selection; background arrivals never join silently.

- [ ] **R42 — Bulk actions:** Browser frozen reviews, approved full-membership actions, owned durable execution, optimistic worker queries, 20-job/50-item History, Pause/Resume, Undo and explicit failure/uncertainty decisions are connected. Identity continuity follows acknowledged moves/validated aliases; receipts retain actual dispatch and inverse identities separately from frozen review metadata. The latest increment paints prepared Undo rows/counts before decision persistence or subsequent queries, preserves newer reader flags and reports rejection after History closes. **Delivered browser increment:** startup saved-group recovery in [`df4f29c`](https://github.com/sam-ruff/shep.so/commit/df4f29c2e12d21fc71353920696dd4958cde363a) uses bounded indexed observations, direct older-group review, retained targets on inspection failure, cache-only retry and ownership-aware tab-loss recovery. All 137 Chromium scenarios and 127 units pass; shipping evidence is in the completion log. **Remaining:** broader Undo lifecycle (changed scope/page, queued approval, partial failure and overlapping groups), abandoned-review/staging cleanup, cross-account transport, large-group performance and native equivalents. Add complete Flutter capture-based review/execution/Undo/History/recovery and native/browser Y/N/Enter/Escape/remappable review-key parity. Preserve optimistic acknowledgement, newer per-field intent, exact membership, source/cache fences, cache-only repair, conservative uncertain restart and provider receipts; never automatically repeat an ambiguous write. Desktop's remaining group/individual coordination and History/recovery audit stays open. Latest evidence and shipping are in docs/COMPLETION.md.

- [ ] **R45 — Drag mail to folders:** drag one mail or a selected group onto sidebar folders; highlight valid destinations, respect cross-account preference/provider support, and use the bulk-move confirmation.

## Folders and composition

- [ ] **R30 — Folder trees:** nested folders form collapsible groups, collapsed by default, honoring each IMAP hierarchy delimiter and nonselectable parents.
- [ ] **R30 — Folder context menus:** delete folders and move them inside another folder; review destructive scope, keep account/server/cache state consistent, and test failure/retry behavior.
- [ ] **R35 — Inline composer:** new messages/replies open in the preview pane; autosave while typing and support switching among multiple drafts and received mail without losing content, recipients or attachments. Browser regression found during read-on-leave verification: multiple saved drafts inherit the three-column mail/5 px divider grid, making one draft overlap another. Saved drafts now have an independently scrollable, clickable list with correct counts; compact Playwright controls reopen every item and the real HTTPS account-removal/draft regression passes. This does not complete inline/multiple-editor parity.

## Account/settings sync and backups

- [ ] **R76 — Scheduled out-of-office replies:** add **Preferences → Automatic replies** for one account, selected account groups or all accounts. Use a list of named reply entries: type the message once, set its schedule, then choose accounts with a searchable multi-select, saved account groups and **Select all**. Add another entry for a different message/group without retyping the first (for example, Work and Personal). Include start/end date and time, a visible timezone, clear active/upcoming/ended status, and easy edit/disable controls. Validate end-after-start and resolve overlapping assignments explicitly. Implement and show provider support/setup and per-account results; unsupported or failed accounts must never appear enabled. Prefer server-side scheduling so replies work while Shep is closed. Cover account/group assignment, scheduling boundaries, timezone handling, overlapping edits, partial failure/retry and client parity in provider/UI/E2E tests.

- [ ] **R75 — Sign in with Google:** replace manual Google token entry with an OAuth **“Sign in with Google”** flow across desktop, Flutter and browser clients. Audit existing Google setup screens and distinguish application OAuth configuration from user access/refresh tokens. Request only the scopes needed by enabled features, keep secure token refresh/revocation and account switching, and test denied/cancelled/expired grants. The browser beta gate alone does not connect Gmail, Calendar or Drive; keep provider access and live verification explicit.

- [ ] **R02/R49 — Continuous Drive sync:** use OAuth and private appDataFolder for accounts and portable preferences. Offer opt-in on the first Google setup; discover an existing setup automatically on another PC; provide an ongoing toggle. Sync appearance, shortcuts, contacts and other portable settings with conflict-safe updates and offline recovery. Keep machine-specific paths/window placement local.
- [ ] **R49 — Account credentials on new PCs:** complete the password-protection decision and secure import into the OS keychain. An asynchronous question asks whether to use an additional sync passphrase (once per new PC) or Google-only protection; no answer has arrived yet. Do not claim password sync is implemented.
- [ ] **R32 — Multiple backup destinations:** polished destination list/setup for Local, Google Drive, S3, FTP/FTPS and SFTP, with independently configurable connections and easy extension points.
- [ ] **R32 — Backup options:** compression and chosen passcode encryption at setup, password prompt on restore, multiple destinations enabled together, independent upload results and configurable per-destination rolling copies (e.g. ten unreadable encrypted archives).

## Storage, appearance and final quality

- [ ] **R64 — GNOME launcher icon:** install the approved dog mark with a transparent background, matching the system theme; verify desktop/dash integration and theme changes without replacing the approved design. There are copies of the transparent icon in the shep.website folder one level up from the root
  Current sibling checkout is `../shep-website`; the inspected `assets/`, `docs/images/` and `website/dist/assets/` logo copies are opaque, as are `../shep-clients/web/public/` copies. Locate the intended alpha variants or remove only the background from the approved design.
  Built-in imagegen cutouts and their prompts are saved under ignored `artifacts/imagegen/launcher-alpha/`. The dark results have ragged edges and were rejected; the light candidate needs exact-silhouette review. No candidate has replaced the approved installed assets. Finish clean extraction before theme integration.

- [ ] **R61 — Download-and-install scripts:** provide directly runnable GitHub raw installer scripts for Linux, macOS and Windows. Default to the user home/equivalent, optionally prompt for all-user/system installation with appropriate elevation, and integrate with GNOME/KDE applications, macOS Applications and Windows Start menu. Put the commands near the top of README as the first install option and in the install docs; test download/extraction/install/update and cancellation using isolated fixtures.

- [ ] **R22 — Encrypted local cache:** encrypt SQLite/mail/attachment data at rest and safely migrate existing personal data; keychain credentials and encrypted backups alone do not fulfill this request.
- [ ] **R23 — Large mail:** remove the incoming 25 MiB, snapshot 256 MiB and preview 32,000-character ceilings using bounded streaming/paging and background large downloads. Large mail must not hold up smaller messages. Avoid replacement arbitrary caps/unbounded RAM allocations. Current reader prerequisite: validate multipart nesting iteratively before invoking the recursive MIME parser, and share representation/CID selection between native Rust and browser WASM. A defensive nesting check does not remove the existing size limits or establish streaming/performance parity.
- [ ] **R25 — Palette editor:** configure primary, secondary, background, surface, text, accent and related theme colors in Preferences, with persistent light/dark schemes and readable states.
- [ ] **R15/R17/R21 — Final usability review:** finish remaining redundant-copy, spacing, icon sizing, alignment and compact/large-font checks, Singular message-count labels are corrected in the HTML-reader work; finish the remaining visual review. Preserve the approved dog logo, WebP assets, mouse usability, draggable panes and clean shadcn-style controls.
  The residual scale-control fragment below filtered settings is fixed in 0d2feb1, with direct renderer and saved native pixel regressions; all 109 native functional scenarios pass and the release is installed. Continue checking the previously reported clipped Shortcuts label and other compact/large-font states in the remaining visual audit.
- [ ] **R01/R06 — Provider/platform completeness:** close remaining POP3 protocol/lifecycle gaps, Google/CalDAV/SMTP integration evidence, independent-process coordination and Windows/macOS execution/distribution checks. Do not present fixture evidence as live-provider verification.
- [ ] **R03/R09 — Final performance gates:** keep measurements deferred while the host is busy; run backend/native gates at the end on an idle host. Do not weaken budgets. Keep quality/release CI disabled until requested; documentation publishing has a separately recorded exception in AGENTS.md. Continuation: exact FTS materialization and the covering unread-account index now pass the unchanged backend budgets (search p95 35.54 ms on 100,000 rows). All 118 functional flows passed before the final index, and all 14 relevant index controls pass with compilation stopped. Native navigation still measures 154.81–162.33 ms against 150 ms across four runs; investigate the retained timing evidence. The combined performance gate remains failing; do not relax it or claim full responsiveness completion.
- [ ] **R08/R10 — Ship verified work:** Push combined client/website/backend work promptly to `feat/mobile-web-clients`; do not wait for full product parity. Run relevant checks and mandatory commit hooks, retain failed evidence and record limits. Do not merge/push clients to main or replace the installed desktop during the worktree review phase. Independent desktop-main shipping follows its own release/install gates. Preserve personal data and keep logs/artifacts outside the root. R78 handover and checkpoint shipping are complete. The user explicitly resumed development on 2026-09-08; continue full parity from the handover, with OAuth/shared profiles now first after the shipped browser recovery increment, and keep pushing verified checkpoints.
